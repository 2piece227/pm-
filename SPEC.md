# 트레이너 매니지먼트 게임 — 개발 인수인계 기획서

> 상태: 기획 확정 / 프로토타입 1차 검증 완료 / 실엔진 이식 대기
> 동봉: `trainer_sim_v2.html` (판단 로직 검증용 프로토타입, 단일 파일)

---

## 0. 한 줄 요약

포켓몬 세계관에 **풋볼매니저(FM) 문법**을 얹은 트레이너 육성 매니지먼트 게임.
플레이어는 트레이너를 직접 조작하지 않고, **소속사 운영자**로서 스카웃·파티 편성·지시만 하고
배틀은 시뮬레이션 결과를 관전한다.

---

## 1. 설계 원칙 (이걸 어기면 프로젝트가 망가진다)

1. **에셋 볼륨을 늘리지 않는다.** 오버월드·맵·이동·전투 애니메이션 없음.
   과거 프로젝트들이 도트/에디터 작업에서 멈췄기 때문에 내린 결정.
2. **로직은 데이터 주도로.** 스탯·천성·스타일은 전부 설정 테이블(JSON)로 분리.
   새 항목 추가가 "테이블에 값 하나 추가"로 끝나야 한다.
3. **시뮬 로직과 표시 로직을 분리한다.** 배틀 엔진은 이벤트 배열만 뱉고,
   그걸 텍스트 로그로 출력할지 애니메이션으로 재생할지는 표시 레이어가 결정.
4. **저볼륨 유지.** 아래 §7의 우선순위를 지킬 것. 한 번에 다 만들지 않는다.

---

## 2. 기술 스택

| 항목 | 결정 |
|---|---|
| 플랫폼 | HTML / JS (브라우저) |
| 배틀 엔진 | `@pkmn/sim` (포켓몬쇼다운 시뮬레이터, MIT) 이식 |
| 포켓몬·기술 데이터 | `@pkmn/dex` 또는 PokeAPI |
| 그래픽 | HGSS풍 고정 도트, 스킬 이펙트 간소화 |
| 개발 환경 | 로컬 프로젝트 + 번들러 (Claude Code로 진행) |

**주의:** 배틀 계산 로직은 API 호출로 해결되지 않는다. 데이터만 API로 오고,
계산은 `@pkmn/sim`을 프로젝트에 통합해야 한다. 브라우저에서 쓰려면 번들링 필요.

---

## 3. 코어 루프

```
트레이너 육성 → 파티 조합 → 배틀 시뮬(관전) → 평판·자금 → 스카웃·유스 → (반복)
```

- **트레이너 육성**: 시작점이자 도착점. 배틀 결과가 성장에 반영된다.
- **파티 조합 → 배틀 시뮬**: 재미가 가장 응축된 구간. 최우선 구현 대상.
- **평판·자금**: 스폰서·모기업·콘테스트를 하나로 묶는 허브.
- **스카웃·유스**: 후반 콘텐츠. 초기 프로토타입에서는 생략해도 루프가 성립한다.

---

## 4. 트레이너 스탯 (확정)

### 4.1 실력 스탯 — 5종, 각각 다른 층위에 작동

| 스탯 | 작동 층위 | 구현 모델 |
|---|---|---|
| **판단력** | 매 턴 행동 선택 | softmax **온도** — 높으면 분포가 뾰족(최적수), 낮으면 평평(오선택) |
| **운영** | 매 턴 자원 관리 | **임계값** — 교체 시점 + 회복약 사용 시점을 함께 결정 |
| **집중력** | 한 배틀 내 시간 경과 | 턴이 지날수록 온도가 상승(감쇠) |
| **지식** | 상대 정보량 | 상성 판정에 노이즈. 낮으면 0배/4배를 오판 |
| **멘탈** | 배틀 간 연속성 | 아군이 쓰러진 직후 N턴간 온도 상승 |

> 5개가 서로 다른 축에 붙어 있어 역할이 겹치지 않는다. 이 분리를 유지할 것.
> "운영"은 교체와 회복약을 하나로 묶은 것 — 둘 다 "지금 버틸까 vs 조치할까"라는 같은 판단이라서.

### 4.2 잠재력 / 실력 (FM의 PA/CA)

- **잠재력(PA)**: 도달 가능한 최대치. 숨김. 훈련으로 오르지 않음.
- **실력(CA)**: 현재 능력치. 훈련·배틀로 PA까지 성장.
- 성장 공식: `성장량 = f(훈련강도, 코치궁합, 만족도, PA−CA 격차)`
  - PA−CA 격차가 클수록 성장 빠름 (초반 쭉쭉, 후반 완만)
  - 만족도 낮으면 성장 계수 페널티

### 4.3 천성 (태생, 성장 안 함)

| 항목 | 적용 지점 |
|---|---|
| **순응도** | `실제확률 = (1−순응도)×기본점수 + 순응도×지시반영점수` |
| **선호도(타입)** | 만족도 계산 + 창고 픽업 제한 |
| **플레이 스타일** | 판단력 점수 계산에 곱해지는 **가중치 벡터** |
| **스타성** | (서브) 콘테스트·포켓우드 평가 |

**플레이 스타일 예시:**
```js
기점형: { 상태변화기: 1.8, 공격기: 0.95, 교체편향: 1.0 }
대면형: { 상태변화기: 0.55, 공격기: 1.3,  교체편향: 0.7 }
균형형: { 상태변화기: 1.0,  공격기: 1.0,  교체편향: 1.0 }
```
스타일은 새 로직이 아니라 **기존 점수식에 곱하는 계수**다. 종류를 늘리려면 벡터만 추가.

### 4.4 스탯 스케일에 대한 미해결 이슈

현재 20점 만점. 프로토 검증 결과 **상위 구간(17~20)이 너무 촘촘해서 차이가 체감되지 않는다.**
- 난천(18/19/18/17/20) vs 단델(올20): 승률 61.2%
- 만점 vs 만점: 65.3%
- → 스탯 3~4 차이가 승률 4p밖에 못 움직임

**100점 만점으로 늘리거나, 상위 구간의 효과 곡선을 비선형으로 만들 필요가 있다.**

---

## 5. 배틀 시스템

### 5.1 재현 범위 (원작 수준 목표)

난수(0.85~1.00), 급소, 명중 실패, 랭크업/다운(−6~+6), 다단히트,
물리/특수 분리, 특성, 도구, 상태이상, 필드/날씨, 우선도.

→ `@pkmn/sim`을 쓰면 이 전체가 이미 구현돼 있다. 직접 짜지 말 것.

### 5.2 AI 레이어 (직접 만들어야 하는 유일한 부분)

엔진이 "이 수를 두면 결과가 이렇다"를 계산하고,
AI 레이어는 **"어떤 수를 둘까"**만 담당한다. 두 레이어는 분리 유지.

**행동 점수 계산:**
```
공격기 점수 = 상성뺀_기대데미지% × 체감상성 × 스타일가중치
              (기대데미지 ≥ 상대 남은HP% 이면 KO 보너스 +42)

기점기 점수 = (배율−1) × 최대타 × 남은턴 − 이번턴_손해
              배율 = 랭크(현재+상승) / 랭크(현재)
              남은턴 = min(상대 잡는 턴, 내가 버티는 턴, 6)

교체 점수 = (위험도 − 임계값) × 30 + (현재피해 − 벤치피해) × 0.42
            위험도 = 예상피해% / 내 남은HP%
            임계값 = 0.85 + (20 − 운영) × 0.06
```

**중요 — 실패했던 접근:**
초기엔 기점기 점수를 `상승폭 × 상수 × HP비율` 같은 임의값으로 뒀다.
1턴만 보는 탐욕적 점수라 기점기가 구조적으로 저평가되어 **한 번도 선택되지 않았다.**
기점기는 본질적으로 다턴 투자이므로 반드시 **남은 턴 수를 반영한 기대이득**으로 계산해야 한다.

**중요 — 지식 스탯 구현 시 함정:**
"상성 적용된 데미지 × 체감상성"으로 계산하면 상성 0배 기술은 데미지가 0이라
후보에조차 오르지 않는다. 지식 낮은 트레이너가 **리자몽에게 지진을 날리는 실수**를
만들려면, **상성을 뺀 맨몸 데미지**를 먼저 구하고 거기에 체감상성을 곱해야 한다.

### 5.3 표시 방식

| 배틀 등급 | 처리 |
|---|---|
| 일반 배틀 | 자동 스킵 + 텍스트 로그만 |
| 체육관 / 리그 / 대회 결승 | 배속 가능한 관전모드 (스킵 가능) |

시뮬 엔진은 하나. **턴별 이벤트 배열**을 뱉고 표시 레이어에서 분기한다.

**이벤트 스키마 (프로토에서 검증됨):**
```json
{ "t":"turn",   "n":1 }
{ "t":"move",   "side":"A", "mon":"드래펄트", "mv":"용성군", "target":"한카리아스",
                "before":100, "after":1, "hits":1, "e":2, "crits":0, "type":"dragon" }
{ "t":"stage",  "side":"B", "mon":"한카리아스", "stat":"방어", "d":-1 }
{ "t":"item",   "side":"A", "mon":"한카리아스", "before":1, "after":51, "left":1 }
{ "t":"ability","side":"A", "mon":"한카리아스", "ab":"까칠한피부", "txt":"..." }
{ "t":"switch", "side":"B", "from":"드래펄트", "to":"킬가르도" }
{ "t":"faint",  "side":"A", "mon":"화강돌" }
{ "t":"end",    "w":"A", "turns":5 }
```

**로그 출력 포맷 (포켓몬쇼다운 리플레이 스타일):**
```
턴 1
드래펄트의 용성군 → 한카리아스  100% → 1%
  효과가 굉장했다!
  드래펄트 특공 -2
한카리아스 회복약 사용  1% → 51%  (남은 개수 1)
한카리아스의 스케일샷 → 드래펄트  40% → 0%  (2번 맞았다)
  드래펄트 쓰러졌다!
단델: 킬가르도 출전!
```

---

## 6. 메타 시스템 (배틀 밖)

### 6.1 소속사 / 트레이너 관리
- 플레이어는 소속사 운영자. 트레이너는 초반 이후 늘어난다.
- **코치 고용** → 일정 등급 이하 트레이너는 FM 유스팀처럼 자동 육성.
  결과 리포트만 뜨고, 등급을 넘으면 "직접 관리하시겠습니까?" 알림.
- **지시 시스템** (FM 팀 지시와 동일 논리 — 조작이 아니라 정책 설정):
  - 배틀 빈도: "하루 6배틀" ↔ "하루 2배틀" (성장↑ vs 피로↑)
  - 센터 방문: "자주" ↔ "최소화" (컨디션 vs 시간)
  - 포획 방침: "경험치 중시" ↔ "특정 포켓몬만"
  - 순응도 낮은 트레이너는 지시를 무시하고 자기 성향대로 행동한다.

### 6.2 포켓몬 창고 (포켓몬고 "박사에게 보내기" 컨셉)
- 소속 트레이너가 포획한 포켓몬 → 소속사 창고로 전송 → 유스의 스타팅/지원용으로 재활용.
- **선호도 기반 픽업 제한 (완전 차단, 페널티 아님)**
  - 포켓몬 종족값으로 성향 태그 자동 분류 (물공우세/특공우세/밸런스형 등)
  - 트레이너 선호와 안 맞으면 박스에서 **빨간색 표시 + 선택 불가**
  - 예: 특공 선호 트레이너 → 후딘 불가, 망나뇽(밸런스형) 가능
  - **주의:** 너무 빡빡하면 "줄 게 없음" 병목 발생. 분류 경계값은 넉넉하게,
    "선호 무시하고 강제 배정"하는 예외 옵션 하나는 남겨둘 것.

### 6.3 자금 / 평판
```
배틀·대회 성적 → 평판↑ → (a) 유망주가 알아서 찾아옴  (b) 스폰서 제안
                        → 스폰서 조건 클리어 → 자금·평판 추가 상승 → 반복
```
- **모기업**: 시작 자금 차등. 캐릭터 선택(난이도)과 연동. 업종별 보너스 가능.
  - 역할 분리 권장: 모기업 = 시작 조건 설정용, 이후는 스폰서/평판이 담당.
- **스폰서**: 기한부 조건 계약. 예) "기한 내 뱃지 3개급 트레이너 3명 배출", 미달성 시 해지.
- **평판 페널티**: 회복약을 남용해 이기면 평판 감점.
  - 정도의 문제이므로 "평균 대비 몇 배 이상"의 기준선이 필요.
  - 반대로 무회복 승리에 "클린 승리" 보너스를 주면 자금 vs 명예 트레이드오프가 생김.

### 6.4 화폐 설계 — 절대값 대신 배수로

배틀 1회 평균 상금을 **100(기준 단위)**으로 두고 나머지를 배수로 설계한다.

| 항목 | 성격 | 권장 배수 |
|---|---|---|
| 식비/생활 경비 | 반복 지출(일일) | 5~10 |
| 코치 영입 | 반복 지출(시즌) | 300~800 |
| CF / 포켓우드 | 조건부 수입 | 200~500 |
| 스폰서 계약금 | 조건부 목돈 | 500~2000 |

**원칙:**
1. 반복 지출 < 반복 수입 (항상 성립). 마진 크기가 곧 난이도.
2. 일회성 수입은 반드시 조건부. 상시화되면 밸런스 붕괴.
3. 코치 같은 투자성 지출은 장기 회수 가능해야 고용할 이유가 생긴다.
4. **절대값 하드코딩 금지.** 기준값 하나만 바꾸면 전체가 스케일되게 할 것.

### 6.5 캐릭터 선택 = 난이도
- 캐릭터마다 시작 스탯 / 시작 포켓몬 / 모기업 / 이벤트 온오프가 다르다.
- 예: 반바지 꼬마 = 오성급 고난도(약체 스타팅, 훈련 자원 제약) /
      난천 = 초보자용(강한 스타팅, 여유 자금)
- **맵·스토리는 전부 재사용**되므로 개발 비용 대비 재플레이성 효율이 가장 좋은 시스템.

### 6.6 서브 (우선순위 낮음 — 나중에)
- **콘테스트 → 평판 → 포켓우드 계약**: 스타성 기반. 배틀 트랙과 별개의 성공 경로.
  배틀은 약해도 스타성 높은 트레이너를 콘테스트로 밀어 평판을 버는 우회 전략이 가능해진다.
- 대회 파견, 네임드 캐릭터 영입 이벤트(평판 임계값 기반).

---

## 7. 구현 우선순위

| 단계 | 내용 | 비고 |
|---|---|---|
| 1 | `@pkmn/sim` 세팅 + 번들링 | 브라우저 동작 확인까지 |
| 2 | AI 레이어 이식 (판단력·운영) | 프로토 코드 재사용 가능 |
| 3 | 팀 빌딩 + 배틀 길이 튜닝 | §8 참조. 여기가 실질 관문 |
| 4 | 집중력·지식·멘탈 검증 | 배틀이 길어져야 비로소 검증 가능 |
| 5 | 천성(순응도·스타일) 연결 | |
| 6 | 육성 루프 (PA/CA, 훈련) | |
| 7 | 평판·자금·스폰서 | |
| 8 | 스카웃·창고·유스·코치 | |
| 9 | 콘테스트·포켓우드 | 서브 |

---

## 8. 프로토타입 검증 결과 (반드시 읽을 것)

동봉된 `trainer_sim_v2.html`로 얻은 결론.

**테스트 매치업**
- 난천: 화강돌(고스트·악 / 프레셔) + 한카리아스(드래곤·땅 / 까칠한피부)
- 단델: 드래펄트(드래곤·고스트 / 클리어바디) + 리자몽(불꽃·비행 / 맹화)
- Lv50, 노력치 0, 무보정, 개체값 31

### ✅ 검증된 것

**판단력·지식은 확실히 작동한다.**
- 스탯 전체 7 → 20으로 올리면 승률 36.8% → 84.5% (48p 폭)
- 지식별 "리자몽에게 지진(무효기)" 오판 횟수 (1200판 기준):
  지식 20 → 0회 / 지식 17 → 44회 / 지식 6 → 408회

**특성·랭크·다단히트·물리특수 분리 전부 정상 동작.**

### ⚠️ 발견된 문제

**(1) 배틀이 너무 짧아 스탯 절반이 놀고 있다 — 최대 이슈**

평균 4.4턴. 실제 수치:

| 한카리아스 vs 드래펄트 | 값 |
|---|---|
| 내 최대타 (스케일샷, 2배) | 100.5%/턴 |
| 상대 최대타 (용성군) | 90.5%/턴 |
| 상대 잡는 턴 | 1 |
| 내가 버티는 턴 | 2 |

한 방에 죽는다. 그래서 **기점기가 수학적으로 절대 선택되지 않는다** (1500판 중 0회).
집중력(장기전 감쇠)·멘탈·교체 판단도 발동할 틈이 없다.

내구 배율을 올려 검증한 결과:

| HP 배율 | 기점형 칼춤 사용 | 대면형 칼춤 사용 | 평균 턴 |
|---|---|---|---|
| ×1 | 0% | 0% | 4.4 |
| ×1.5 | 0% | 0% | 9.3 |
| ×2 | 10.9% | 0% | 11.7 |
| ×3 | 32.0% | 0.1% | 9.1 |

**내구 ×2부터 기점형이 칼춤을 추고, 대면형은 안 춘다 — 스타일 차이가 여기서 처음 발현된다.**

> 이건 공식 오류가 아니다. 원작 공식 그대로이며, 실제 대전에서도 이 구성은 원턴킬이 난다.
> 배틀 길이를 결정하는 건 엔진이 아니라 **팀 빌딩**(노력치 방어·HP 투자, 도구, 6마리 파티,
> 상태이상, 성격 보정)이다. `@pkmn/sim`을 붙여도 팀을 이렇게 짜면 여전히 4턴에 끝난다.

**→ 목표: 평균 8~12턴. 노력치·도구·6마리를 붙여 자연스럽게 도달시킬 것.**

**(2) 내구를 올리면 이번엔 회복약이 깨진다**

내구 ×1.5 이상에서 난천 승률이 98%로 폭등. 화강돌이 108/108로 너무 단단하고
회복약 2개가 장기전에서 압도적이 된다. 내구 ×3에선 저주가 100% 배틀에서 등장.
**배틀 길이를 늘릴 때 회복약 개수·회복량도 함께 재조정해야 한다.**

**(3) 물리/특수 판별 실험은 다시 설계해야 한다**

화강돌이 방어 108 / 특방 108로 완전 대칭이라 판별력이 없다.
드래펄트가 섀도클로(물리)냐 섀도볼(특수)냐를 고를 때 점수가 8400 대 8000으로 붙는다.
**한카리아스(115/105)처럼 비대칭인 포켓몬으로 실험해야 한다.**

**(4) 저주는 기점기가 아니다**

화강돌이 고스트 타입이라 저주는 랭크업기가 아니라
**자기 HP 절반을 태워 상대에게 턴당 25% 도트**를 거는 기술이 된다.
화강돌 종족 HP가 50이라 절반이 너무 비싸 AI가 합리적으로 회피한다(만점 기준 0회).
기점 실험용 카드로는 부적합.

---

## 9. 미결정 사항

- 스탯 스케일 (20점 → 100점 확대 여부, 상위 구간 곡선)
- 목표 배틀 길이 확정 및 그에 맞는 회복약 밸런스
- 유망주 유입 방식 (스카웃 화면 후보 노출 vs 자동 합류)
- 동시 다중 파견 가능 여부 (여러 트레이너 스케줄 병렬 운용)
- 스폰서 다중 계약 허용 여부
- 지시 변경 빈도 (매일 vs 며칠 단위 고정)
- 콘테스트 평가 기준 (스타성 단독 vs 원작 5카테고리)

---

## 10. 리스크

**포켓몬 IP 팬게임이라 상업화 불가이며, 저작권자가 배포 중단을 요구할 수 있다.**
포트폴리오·홍보 목적이라면 이 시스템 자체는 포켓몬이 아니어도 성립한다는 점을 고려할 것 —
오리지널 IP로 껍데기만 바꾸면 저작권 문제가 사라지고, 상업화와 외부 승인 모두 가능해진다.
`@pkmn/sim`은 MIT라 엔진 자체는 문제없으나, 포켓몬 이름·스프라이트·기술명은 별개 문제다.
