# 트레이너 매니지먼트 게임 — 개발 인수인계 기획서 v4

> 상태: 실제 빌드(`2piece227.github.io/pm-`) 프로토타입 진행 중. 오프닝 시퀀스(체육관 뱃지)와 연간 시즌 사이클 신규 확정. 실빌드에서 발견된 버그/설계 공백 반영.
> 동봉: `trainer_sim_v2.html` (판단 로직 + 실제 배틀 엔진 검증용 프로토타입, 단일 파일). 실 빌드는 이 프로토타입을 기반으로 `@pkmn/sim`을 통합해 별도 저장소에서 진행 중.
> v3 대비 변경: §3.10(신규) 오프닝 시퀀스, §5.6(신규) 연간 시즌 사이클, §5.1 챔피언/사천왕 로테이션 로직 추가, §6.1 로스터 잠금 조건 추가, §8 스프라이트 세대 폴백 확인, §7.6(신규) 아이템(회복약) 엔진 통합 공백, §14에 실빌드 버그 패턴 추가.

---

## 0. 설계 원칙

### 0.1 유지되는 원칙

1. **에셋 볼륨을 늘리지 않는다.** 오버월드·맵·이동·전투 애니메이션 없음. 배틀은 관전형(스킵·배속 가능), 스프라이트는 기존 애니메이션 리소스 재사용(§8 참고). 단, §3.10 오프닝 시퀀스의 "맵으로 파견/도전 보내기"는 예외적으로 간단한 리스트/맵 UI를 허용한다 — 새 그래픽 리소스 없이 기존 컴포넌트로 구성할 것.
2. **로직은 데이터 주도로.** 스탯·천성·스타일·소속사·리그·시즌은 전부 설정 테이블(JSON)로 분리. 새 항목 추가가 "테이블에 값 하나 추가"로 끝나야 한다.
3. **시뮬 로직과 표시 로직을 분리한다.** 배틀 엔진은 이벤트 배열만 뱉고, 표시 레이어(로그/관전/뉴스 요약)가 그걸 어떻게 보여줄지 결정한다. 이 패턴은 배틀뿐 아니라 **하루 단위 진행에도 동일하게 적용**한다 (§9).
4. **플레이어 중심이 아니라, 이미 굴러가는 세상에 플레이어가 들어온 느낌이어야 한다.** NPC 소속사는 플레이어가 안 봐도 스카웃·방출·대회 참가를 계속한다. 대회는 플레이어와 무관하게 자주 열린다. 네임드는 플레이어를 기다리지 않는다. 전설의 포켓몬도 플레이어를 기다리지 않는다(§11).

### 0.2 "저볼륨 유지"를 대체하는 구현 원칙 — 중요, 반드시 읽을 것

> v1에서는 "저볼륨 유지"를 추상 원칙으로 뒀는데, 이게 **"일단 얕게/작게 구현해라"로 해석되어 재작업을 유발**하는 문제가 있었다. 얕은 임시 구조를 만들고 나중에 실제 스펙이 들어오면 갈아엎는 식으로는 안 된다.

**대신 이 원칙을 따른다:**

- **데이터 구조(스키마)는 이 문서 전체가 설명하는 최종 형태로 먼저 설계한다.** 트레이너 객체, 소속사 객체, 리그 객체, 코치 객체, 시즌 객체 등은 처음부터 여기 적힌 필드를 다 갖춘 형태로 만든다. 아직 안 쓰는 필드가 있어도 상관없다.
- **기능 구현(로직)만 §13의 우선순위 순서로 채운다.** 우선순위가 낮은 기능은 필드는 존재하되 로직이 비어있거나 기본값을 반환하는 상태로 둔다.
- **이렇게 하면 나중 단계에서 스키마를 다시 설계할 일이 없다.** "일단 만들고 나중에 갈아엎기"가 아니라 "다 짜놓고 순서대로 채우기"가 원칙이다.
- 예: 트레이너 객체에 `coachAssigned`, `mentalDebuff`, `contractBuyout`, `badgeProgress`, `youthStatus` 필드는 1단계부터 존재해야 한다. 그 필드를 실제로 채우는 로직은 나중 단계에 구현해도 된다.

---

## 1. 기술 스택

| 항목 | 결정 |
|---|---|
| 플랫폼 | HTML / JS (브라우저) |
| 배틀 엔진 | `@pkmn/sim` (포켓몬쇼다운 시뮬레이터, MIT) — **실제 통합 완료, 실빌드에서 작동 확인됨** |
| 포켓몬·기술 데이터 | `@pkmn/dex` 또는 PokeAPI |
| 스프라이트 | 쇼다운 gen4(HGSS 세대) 도트 링크. **그 세대에 없는 종은 상위 세대로 자동 대체**(실빌드에서 확인, §8) |
| 개발 환경 | 로컬 프로젝트 + 번들러, Claude Code로 진행 중 |

배틀 계산 로직은 API 호출로 해결되지 않는다. 데이터만 API로 오고, 계산은 `@pkmn/sim`을 프로젝트에 통합해야 한다.

---

## 2. 코어 루프

```
[최초 1회] 오프닝 시퀀스 — §3.10 참고

[플레이어 루프, 오프닝 이후]
트레이너 육성 → 파티 조합 → 배틀 시뮬(관전) → 평판·자금 → 스카웃·유스 → (반복)

[세계 루프 — 플레이어와 병행, 항상 돌아감]
NPC 소속사 스카웃·방출 → 대회 참가 여부 자체 판단 → 대회 시뮬(경량) → 뉴스 요약
NPC의 전설의 포켓몬 포획 시도(극저확률) → 성공 시 로스터 편입(§11)

[하루 단위]
행동 소비(배틀/훈련/파견/코치 배정) → 이벤트 누적 → Day 종료 → 뉴스탭 요약

[연간 단위 — §5.6]
시즌 전반부(뱃지/로컬컵) → 시즌 후반부(지방 리그) → 글로벌 월즈 → 스토브리그 → (다음 시즌)
```

---

## 3. 소속사 시스템

### 3.1 세계관 전제

- 소속사 제도는 **플레이어가 처음 만든 게 아니라, 각 지역 리그에 이미 자리잡은 상태**로 시작한다. "세상에 들어온 느낌"(§0.1-4)을 위한 핵심 전제.
- 단, 리그의 **이번 시즌은 첫 시즌**이라는 설정을 둔다 — §3.10 오프닝 시퀀스가 이 설정 위에서 성립한다 (모두가 아직 안 커봤으므로 로스터가 작다는 서사적 근거).
- 실프·데본·매크로코스모스 같은 원작 대기업급 존재를 **스케일 기준점**으로 삼되, 직접 조작 가능한 라이벌로 만들지는 않는다 (IP 리스크 축소, §15).

### 3.2 리그 구조

- 지역(관동·성도는 하나의 리그로 묶임, 호연, 신오 등)마다 **리그**가 존재하고, 리그당 소속사는 **4~5개**.
- 리그 규모가 4~5개인 이유: 이 숫자가 §3.4 "정밀 계층"에 들어가는 대상 수의 상한이다. 그 이상이면 계산 비용이 §0.2 원칙과 부딪힌다.
- 소속사 등급(스케일)은 4단계: **대기업 / 세미대기업 / 중견 / 중소 / 자체**(자체는 등급이 아니라 플레이어 시작 옵션 중 하나 — §3.3 참고).
- **등급 판정 기준(일관 적용):** 원작에서 실제로 차지한 영향력 범위(지역 수, 점거·점령 이벤트 유무)에 비례해 등급을 매긴다.
  - 예: 로켓단은 관동·성도 두 지역에 걸쳐 있고 실프 본사 점거 등 점령 이벤트가 있어 **세미대기업급**.
  - 아쿠아단/마그마단은 호연 한 지역, 거점도 하나씩이라 **중견급**.
  - 이 기준을 이후 지역(신오 갤럭시단, 하나 플라즈마단 등)에도 동일 적용한다 (§10 참고).

### 3.3 캐릭터(소속사) 선택 = 난이도

플레이어는 리그 진입 시 소속사 하나를 선택한다. 이 선택 하나가 아래 세 가지 축을 동시에 결정한다 — **별도 난이도 시스템을 안 만들어도 되는 이유**.

| 축 | 자체(모기업 없음) | 중소/중견 | 대기업/세미대기업 |
|---|---|---|---|
| 시작 자금 | 최소 | 중간 | 넉넉함 |
| 코치진 | 0명, 직접 계약해야 함 (§6) | 소수 | 이미 여럿 배정됨 |
| 이사진 기대치 | 없음 | 낮음~중간 | 높음 |
| 실패 시 결과 | 파산(소속사 해체) | 경질→재취업 | 경질→재취업 |

- **자체**는 "잘라줄 이사진이 없으므로" 실패가 경질이 아니라 파산으로 처리된다. 부수 효과로, 코치 없이 전 시스템을 직접 다뤄야 해서 튜토리얼 역할도 겸한다.
- **경질**은 하드 게임오버가 아니라 **더 낮은 등급의 다른(빈) 소속사로 재취업**하는 구조 (트레이너들은 원래 소속사에 남고 플레이어만 이동, 커리어는 계속됨). FM에서 감독이 경질되고 하위 리그 팀 제안을 받는 것과 동일한 논리.
- 기대치 조건(예: "이번 시즌 리그 3위 이내")은 §5.4 스폰서 계약과 동일한 구조를 재사용한다 — 새 시스템 아님.
- 소속사 등급과 무관하게, **모든 소속사는 §3.10 오프닝 시퀀스(로스터 1명 + 뱃지 8개)를 거쳐야 로스터가 확장된다.**

### 3.4 정밀/경량 시뮬 계층

계산 비용을 관리하는 핵심 원칙.

| 계층 | 대상 | 처리 |
|---|---|---|
| **정밀** | 플레이어와 같은 지역 리그의 소속사 전원(4~5개) | 풀 AI(판단력·운영 등, §4) + 실제 배틀 엔진(§7). §12의 "플레이어 맞춤 견제"도 이 계층에서만 작동 |
| **경량** | 타 지방 리그의 소속사 | 스탯 합/평판 비교만으로 스카웃·방출·대회 성적을 확률 계산. 배틀 시뮬 안 돌림 |

- 경량 계층 소속사가 **글로벌 대회(§5.2)에서 플레이어와 실제로 마주치는 순간에만 정밀 계층으로 일시 승격**시켜 계산한다.
- 경량 계층의 스카웃/방출 로직 예시:
  ```
  매 판단 주기: 자금 여유 있고 + 시장에 평균보다 스탯 높은 유망주 있음 → 영입 시도
             트레이너 성적 오래 부진 + 만족도 낮음 → 방출
  ```

### 3.5 NPC 대회 참가 판단

NPC 소속사도 §4.4 지시 시스템과 같은 정책 기반 판단 함수를 공유한다 (대상만 플레이어→NPC로 확장).

```
참가 점수 = 우승 가능성(자기 스탯 vs 대회 평균 스탯)
          × 대회 보상(상금/평판)
          − 참가 비용(이동 경비, 컨디션 소모)
          × 소속사 성향(공격적 확장형 vs 안정 운영형)
```

스탯 낮은 NPC는 하위 대회만, 강한 NPC는 상위 대회만 노리게 되어 **참가 자체가 자연스러운 필터링**이 된다 (FM에서 약팀이 챔스에 못 나가는 것과 동일). §5.2의 메이저 대회 인원 제한과 결합하면, 강한 소속사도 슬롯 수 이상은 못 낸다.

### 3.6 관동·성도 리그 라인업 (확정)

| 소속사 | 등급 | 컨셉 |
|---|---|---|
| 실프주식회사 | 대기업 | 정통 육성, 원작 그대로의 관동 최대 기업 |
| 로켓단 | 세미대기업 | 순응도 높은 트레이너 우선 스카웃, 조무래기 양성 목적, 만족도/평판 페널티 무시, 위약금 협상이 지저분함(낮은 금액 강행). 게임코너는 위장 거점으로 배경 설정에 흡수 |
| 무지개 코퍼레이션 | 중견 | 백화점이 아니라 관동 프렌들리숍 납품 유통망 — 자금은 있지만 육성 노하우 약함 |
| 도라지 트레이더스 | 중소 | 백화점(도라지시티) 중심, 성도 지역색 |

라디오타워는 소속사가 아니라 §5.5 콘테스트/포켓우드 트랙의 CF·미디어 계약처로 별도 취급. 사파리존·게임코너는 소속사 컨셉과 안 맞아 제외(게임코너는 로켓단 배경으로 흡수).

**네이밍 원칙(중요, 오해 방지):** 무지개/도라지/카제노 같은 자체 생성 기업명은 **"유래가 있다" 정도의 배경 설정일 뿐, 게임플레이나 발견 유도 장치가 아니다.** 목표는 "포켓몬 얕게 아는 사람은 새 이름으로 받아들이고, 포덕은 알아채면 반가운" 정도의 이스터에그이지, 이걸 찾아야 하는 퀘스트나 UI 힌트를 만들 필요는 없다. (예: 카제노 모터스의 "카제노"는 호연 자전거포 주인 라이델의 일본 원어명 — 실제 검증됨. 원규(호연 바위타입 관장, 금탄체육관)처럼 원작 정식 캐릭터명도 이 원칙과 별개로 그대로 사용 가능.)

### 3.7 호연 리그 라인업 (확정)

| 소속사 | 등급 | 컨셉 |
|---|---|---|
| 데본 코포레이션 | 대기업 | 정통 육성, 호연 최대 기업 |
| 아쿠아단 | 중견 | 바다 확장 이념, 마그마단과 대립 |
| 마그마단 | 중견 | 육지 확장 이념, 아쿠아단과 대립 |
| 카제노 모터스 | 중소 | 호연 자전거샵 겸 이동 특화. 겉보기엔 소매점이나 숨은 모기업이 있다는 컨셉 — 모기업 시스템(§3.3)의 존재를 서사로 보여주는 장치 |

### 3.8 신오 리그 — 미정

신오는 관동(실프)·호연(데본)처럼 원작에 확실한 "대기업급" 요소가 없다. 후보:

- **갤럭시단** — 특수목적(세미대기업급 후보, §3.2 기준상 신오 전역 활동 + 본부가 정식 빌딩이라 "위장 기업" 컨셉과 잘 맞음)
- **장막시티 백화점** — 신흥 자금중심
- **연고시티 신문방송국** — 라디오타워처럼 CF/미디어 트랙으로 분리

**대기업 자리는 자체 생성 기업으로 채우는 것을 검토 중** (신오는 신화·고대유적 테마가 강하므로 그쪽 컨셉의 창작 기업이 자연스러울 수 있음). 다음 세션에서 확정 필요.

### 3.9 영입 경쟁과 밀집 문제

**문제 상황:** 자금 많은 소속사(예: 실프)가 무소속 강자(목호·레드·심향 등)를 계속 흡수하면, 시간이 지날수록 한쪽으로만 전력이 쏠려 다른 소속사의 영입 재미가 사라지는 스노우볼 위험이 있다.

**채택된 해법 — 메이저 대회 소속사별 출전 인원 제한 (§5.2):**

```
실프가 강자를 아무리 모아도, 메이저 대회엔 소속사당 최대 N명만 출전 가능
→ N명을 넘는 인원은 대회 성적에 기여 못 함 → 무한정 쌓는 유인이 사라짐
```

- 이게 자금 조절보다 확실한 이유: 자금을 아무리 써도 슬롯이 안 늘어나므로 자금력 자체가 특정 지점에서 무력화된다.
- **로컬 대회는 인원 제한 없음, 글로벌 대회만 제한** — §5.2 로컬/글로벌 구분과 자연스럽게 연동.
- 슬롯보다 강자가 많으면 소속사 내부에서 "누굴 보낼지" 선발전이 생기고, 이걸 플레이어가 직접 고르게 하면 §4.4 지시 시스템처럼 콘텐츠가 된다. NPC도 §3.5 로직으로 내부 선발하며, 그 결과는 §9 뉴스탭 소재가 된다.
- **미정:** 정확한 인원 제한 수(2명? 3명?), 소속사 등급별 슬롯 차등 여부 (§16).

### 3.10 오프닝 시퀀스 — 로스터 1명 → 뱃지 8개 → 확장 (NEW, 중요)

**게임은 항상 트레이너 1명으로 시작하며, 그 트레이너가 지역 뱃지 8개를 모두 딸 때까지 로스터를 늘릴 수 없다.** §3.1의 "이번 시즌은 첫 시즌" 설정이 이 구조의 서사적 근거다.

```
[1막 — 오프닝]
트레이너 1명, 코치 없음(또는 §3.3 등급에 따른 초기 코치)
  ↓ 플레이어가 맵 UI로 직접 체육관을 돌며 도전 (§0.1-1 예외 허용 UI)
지역 뱃지 8개 전부 획득
  ↓
"로스터 확장 + 유스 시스템 개방" 이벤트 발생
  ↓
[2막 — 본편]
이 시점부터 §3~§12에서 정의한 매니지먼트 루프(스카웃/코치/유스/대회) 전면 개방
```

**진입 흐름 (실빌드 확정):**

```
타이틀 (아무 키)
  ↓
대표 이름 입력        ← 특정 문구를 넣으면 개발자 모드로 기존 시뮬 화면 직행 (숨김 진입점)
  ↓
소속사 선택 (§3.3)    ← **등급 라벨을 화면에 절대 안 보여준다.** 모기업 자금 / 이사진 기대만 막대로
  ↓
튜토리얼              ← 기존 화면 위에 말풍선만 얹는다. 새 UI 안 만든다
  ↓
첫 유스 계약 (§5.3)   ← 원클릭 영입 금지. 조건을 걸고 협상한다
  ↓
[본편] 로스터 잠금 상태로 시작 — 뱃지 8개까지 추가 계약 불가
```

- 세이브는 오프닝 선택과 **버전이 있는 전체 스냅샷**을 저장한다. 날짜·포켓몬·기술·계약·자금·난수 위치를 보존하며, 예전 오프닝 선택만 있는 저장도 읽는다.

**설계 근거:**
- **손맛(맵 돌기)과 매니지먼트(위임)가 서로 다른 단계에 배정돼 충돌하지 않는다.** 1막엔 위임할 대상 자체가 없고(트레이너 1명), 2막엔 이미 손맛을 겪었으므로 위임이 자연스럽다.
- 1막이 그대로 **튜토리얼**을 겸한다 — 육성·배틀·파견 시스템을 몸으로 익히게 됨.
- §0.2 원칙에 따라, 로스터 확장·유스·코치 관련 로직은 **1막 완료 전까지 스키마만 존재하고 작동하지 않아야 한다.**

**체육관 뱃지 시스템:**
- 관동·성도 리그는 각 8개씩, 총 16명의 체육관 관장(원작 이름·전문 타입·사용 포켓몬 그대로)을 데이터화한다. 정확한 명단은 원작 데이터 기준으로 채울 것 (예시 검증됨: 호연 금탄체육관 관장 원규, 바위 타입, 꼬마돌·코코파스 사용 — 다른 지역도 이 정밀도로 채울 것).
- 뱃지는 **트레이너 개인 소유**다 (원작과 동일). 소속사 단위 공동 진행이 아니다.
- 뱃지 도전은 §5.2의 로컬 대회와는 별개 트랙이지만, 대진·판정 로직(§7 배틀 엔진)은 그대로 재사용한다.
- 도전 순서는 원작처럼 부분적 자유 진행을 허용한다(고정 아님).

**오프닝 이후 — 유스 트레이너의 뱃지 진행:**
- 2막부터 합류하는 신규 트레이너(유스)는 뱃지 진행이 **코치에게 위임되어 경량 처리**된다 (§6.1 유스 자동 육성과 동일 원칙).
- 유스는 **유스 대회**라는 별도 대회 등급에서만 뛴다 (§5.2 등급 체계에 "유스" 등급 추가 — 하급보다 아래, 유스 신분 전용).
- **유스가 뱃지 8개(졸업 조건) 달성 시**, 플레이어에게 선택지 제시:
  - **콜업**: 유스 플래그 해제, 정식 로스터 편입 (§6.1 "직접 관리 전환" 알림과 동일 로직)
  - **판매**: §5.3 위약금/바이아웃 구조 재사용, NPC 소속사에 이적
  - **수납**: 결정 보류, 유스 상태 유지 (새 상태값 불필요, 다음 판단 주기에 재질문)

**뉴스탭 노출 리듬 (중요):**
- 유스 영입 시점에 "○○ 영입" 뉴스 1회.
- 이후 진행 상황은 **뱃지 1~2개 구간에서는 조용히, 6~7번째 �드지 구간부터 몰아서** 노출 ("민재, 마지막 체육관을 눈앞에 두다", "7번째 체육관 격파!"). 매 뱃지마다 알리면 예측 가능해져 "뜬금포" 효과가 죽는다 — 의도적으로 후반에 밀집시켜 서프라이즈감을 살릴 것.
- 졸업 시 "졸업했습니다 + 선택지" 뉴스/알림으로 마무리.

---

## 4. 트레이너 스탯

### 4.1 실력 스탯 — 5종, 각각 다른 층위에 작동

| 스탯 | 작동 층위 | 구현 모델 |
|---|---|---|
| **판단력** | 매 턴 행동 선택 | softmax **온도** |
| **운영** | 매 턴 자원 관리 | **임계값** — 교체 + 회복약 사용 시점. **실빌드 기준 현재 교체 판단에만 연결돼 있음, 회복약 판단은 미구현(§7.6)** |
| **집중력** | 배틀 내 시간 경과 | 턴 경과에 따른 온도 상승(감쇠) |
| **지식** | 상대 정보량 | 상성 판정 노이즈 (0배/4배 오판 가능) |
| **멘탈** | 배틀 간 연속성 | 아군 쓰러진 직후 온도 상승 + §4.5 연패 페널티 감쇠 |

### 4.2 스탯 비선형 곡선 — 중요

**20을 기준점으로 삼는 비선형 곡선**을 전 스탯에 적용한다.

```js
function curve(v){ return 20 * Math.pow(Math.max(0,v)/20, 1.8); }
```

- v=20에서는 원래 값 그대로 유지(기준점 불변).
- v<20은 더 빨리 깎임 → 일반 트레이너 상단 구간(17~20)의 차이가 벌어짐.
- v>20(네임드 전용, 슬라이더 상한 30까지 실험됨)은 계속 이득이 붙음 → "역대급 천재"를 표현 가능.
- **검증 결과:** 올20 vs 올20 기준 승률 62.6%에서, 올20 vs 네임드(올25)는 87.7%, 올20 vs 네임드(올28)는 93.2%까지 벌어짐. 곡선 적용 전(선형)에는 17~20 차이가 승률 5%p도 못 움직였음.
- **부수 발견:** 양쪽 다 고스탯(예: 올28 vs 올28)이어도 팀 자체의 매치업 우위가 있으면 승률이 50:50으로 안 좁혀지고 오히려 더 벌어진다(노이즈가 줄어들수록 원래 유리한 쪽이 더 확실히 이김). 네임드 매치업 밸런싱 시 유의.

### 4.3 잠재력 / 실력 (FM의 PA/CA)

```
성장량 = 기본성장률 × 멘토코치능력치(§6.2) × 코치궁합 × 만족도계수 × (PA−CA 격차 보정)
```

- PA(잠재력): 숨김, 훈련으로 안 오름. CA(실력): 훈련·배틀로 PA까지 성장.
- PA−CA 격차가 클수록 성장 빠름(초반 쭉쭉, 후반 완만). 만족도 낮으면 성장 계수 페널티.
- **멘토 코치의 능력치**와 코치궁합은 별개 곱연산 항으로 분리 — "실력은 좋은데 이 트레이너랑 안 맞는 멘토" 같은 조합 표현 가능. (§6 코치 시스템 재작성으로, 이 능력치는 이제 "멘토" 역할 코치 전용이다.)
- 포켓몬 자체의 개체값·성격 성장은 §6.2 포켓몬 훈련관이 담당하며, 이 공식과는 별개 트랙이다.

### 4.4 천성 (태생, 성장 안 함)

| 항목 | 적용 지점 |
|---|---|
| **순응도** | `실제확률 = (1−순응도)×기본점수 + 순응도×지시반영점수` |
| **선호도(타입)** | 만족도 계산 + 창고 픽업 완전 차단(페널티 아님) |
| **플레이 스타일** | 판단력 점수 계산에 곱해지는 가중치 벡터 (기점형/대면형/균형형) |
| **스타성** | (서브) 콘테스트·포켓우드 평가 |

지시 시스템(FM 팀 지시와 동일 논리, 조작이 아니라 정책 설정):
- 배틀 빈도, 센터 방문, 포획 방침 등을 지시.
- **요청 주기는 1일/3일/7일 중 선택** — 매일 요청 시 플레이어 피로 우려 반영.
- 순응도 낮은 트레이너는 지시 무시하고 자기 성향대로 행동.

### 4.5 연패 페널티 & 회복 (부상 시스템 없음)

- 부상 시스템은 넣지 않음. 대신 **연패 시 일시적 스탯 하락**, 멘탈 높을수록 감소폭 작음.
- 회복 경로 3가지, 서로 다른 트레이드오프:

| 경로 | 대가 | 속도 |
|---|---|---|
| 자연회복 | 없음(대기) | 느림 |
| 쉬운 상대 파견 | 이동 경비 + 기회비용 | 중간 |
| 휴식 요구 승인 | 며칠간 활동 중단(수입 없음) | 빠름 |

- 지시 주기 도중에도 **조기 개입 가능** (연패 감지 시 알림).
- 휴식 요청을 거절하면 만족도 하락. 만족도가 음수까지 떨어지면 계약 해지 요청/이탈.
- **거절 반응은 만족도×순응도 조합에 따라 다름:**
  - 만족도 높음 + 순응도 높음: "힘들지만 팀의 선택을 존중합니다" (수긍, 만족도 소폭 하락)
  - 만족도 낮음 + 순응도 높음: "…알겠습니다" (겉으론 수긍, 속으론 만족도 더 크게 하락 — 티 안 나게 곪는 유형, 가장 위험함)
  - 만족도 높음 + 순응도 낮음: 직설적이지만 아직 파국은 아님
  - 만족도 낮음 + 순응도 낮음: "저를 존중하셔야 합니다" (직접 반발, 만족도 급락)

---

## 5. 대회 & 계약 시스템

### 5.1 네임드(챔피언·사천왕·관장 등)

- **시간이 지나도 스탯은 성장하지 않는 고정값**으로 확정 (공진화 모델 기각 — 계산 비용 대비 효과 낮음).
- 스탯 곡선(§4.2)의 20 초과 구간이 네임드를 표현하는 실제 도구가 됨.
- **로드아웃(메가진화 등 장비/폼)은 시즌 진행에 따라 갱신될 수 있음** — 이건 스탯 성장이 아니라 §12 NPC 성장/적응 시스템 참고.
- **플레이어가 네임드를 영입해 자리가 비면, 비슷한 스탯의 자체 생성 캐릭터로 자동 대체.** (영입해도 세계관이 즉시 쉬워지지 않게 하는 장치 — FM에서 약팀 맡고 바로 챔스 우승은커녕 출전도 안 되는 것과 같은 논리)
  - 후임 스탯은 원래 네임드의 ±10~15% 편차로 생성 권장(완전 동일하면 위화감, 너무 벌어지면 급약화 위화감) — 세부 수치는 §16 미결정.

**챔피언·사천왕 로테이션 (NEW, §5.6과 연동):**

```
순위 1위 = 챔피언, 2~5위 = 사천왕 (총 5자리 — 1+4로 정확히 맞아떨어짐)

밀려난 사천왕 → 다음 시즌 플레이인(선발전)부터 재도전
새로 진입한 사천왕 → 시드권으로 바로 본선行 (플레이인 면제)
전 챔피언 → 타이틀을 잃어도 무조건 자동 출전 (레전드 취급)
```

- 사천왕 자리가 밀려나는 경우, §5.1의 "네임드 대체" 로직과 대칭으로 처리한다 — 밀려난 자리가 원래 네임드(고정 캐릭터)였다면 그 캐릭터는 강등된 일반 NPC로 취급, 새 강자가 그 자리를 차지.
- 이 로테이션 자체가 §9 뉴스탭의 좋은 소재가 된다 ("○○이(가) 사천왕 자리에서 물러났다", "○○, 플레이인부터 다시 시작").

### 5.2 대회 구조 — 로컬 vs 글로벌, 인원 제한

| 구분 | 자격 조건(후보) | 빈도 | 소속사별 인원 제한 |
|---|---|---|---|
| 유스 대회 | 유스 신분 트레이너 전용 (§3.10) | 잦음 | 없음 |
| 로컬 대회(하/중/상급) | 지역 소속, 자격 레이팅 구간 | 잦음 | 없음 |
| 지방 리그(사천왕·챔피언전) | 해당 지역 뱃지 8개 전부 획득 (§3.10) | **연 1회, 시즌 후반부** (§5.6) | 해당 없음(개인전 성격) |
| 글로벌 월즈 | 지방 리그 성적 기반, 챔피언은 자동 시드 | 연 1회 | **있음 (§3.9)**, 챔피언은 인원 제한과 별개로 자동 출전 |

- **빈도는 잦지만 아무나 다 출전 가능한 건 아님** — §0.1-4 원칙을 대회 시스템에도 강제 적용. 필터 후보: 뱃지 개수, 최근 성적(연패 중 예선 탈락), 소속사 평판 등급, 중복 출전 불가.
- **글로벌 대회는 소속사당 최대 출전 인원을 제한한다.** §3.9에서 정리한 대로, 이게 영입 밀집 문제의 핵심 해법이다. 정확한 인원 수는 §16 미결정.
- **"후보(출전 보장)" 개념은 폐기.** 인원 제한이 밀집 방지 문제를 더 직접적으로 해결한다.
- **지방 리그 도전 자격은 "해당 지역 뱃지 8개 전부"** — 별도 재도전 쿨다운 설계는 불필요하다. 시즌 자체가 연 1회이므로 자연스럽게 다음 시즌까지 재도전이 제한된다.

### 5.3 소속사 계약 (트레이너 개인 계약)

최소 세트:

```
계약 기간
숙식비/경비 지원 비율 (@%)
승리 상금 분배 (소속사:트레이너 = @:#)
패배 상금 부담 비율 (@%)
대회 상금 분배 (소속사:트레이너 = @:#)
위약금(바이아웃)
```

- **성과 보너스 조항 / 독점 조항**은 §3.3의 모기업 기대치·스폰서 계약(아래) 구조를 재사용 — 별도 설계 불필요.
- **위약금은 자동 계산이 아니라 소속사가 임의로 부르는 값.** 소속사 성격(자금 많은 곳 vs 알뜰한 곳)을 드러내는 수단.
- **바이아웃 제안이 오면 트레이너 만족도에 따라 수락/거부가 갈림.**
  - 만족도 높음: 어지간한 금액엔 거절(충성도)
  - 만족도 낮음: 낮은 제안에도 흔들림
  - 순응도 낮음: 소속사 의견 무시하고 본인 의사대로 결정 (플레이어가 붙잡으려 해도 안 먹힐 수 있음)
  - 제안이 오면 **사전 알림 → 만족도 올려서 막을 기회** 부여 (§4.5 조기 개입과 동일 원칙 적용)

#### 5.3.1 유스 계약 (오프닝 첫 계약 / §3.10)

정식 계약과 **항목이 다르다.** 신규 영입이라 남의 소속사에서 빼오는 게 아니므로 **위약금이 없다.**

| 항목 | 뜻 |
|---|---|
| 주급 | 매주 나가는 고정 보장 급여 |
| 영입 개런티 | 계약 시 1회 지급하는 사이닝 보너스 |
| 프로 승급 시 급여 | 유스 졸업 후 주급이 오르는 비율 (%) |
| 뱃지 달성 보너스 | 체육관 뱃지 1개당 지급 |
| 계약 기간 | 년 |

**수락 판정 — 후보마다 가중 선호 프로필을 갖는다.**

```
후보.선호 = {
  평판:        { 기준, 중요도 },   ← 제안으로 못 바꾸는 항목. 간판을 보는 후보용
  주급:        { 기준, 중요도 },
  영입개런티:  { 기준, 중요도 },
  프로승급:    { 기준, 중요도 },
  뱃지보너스:  { 기준, 중요도 },
  계약기간:    { 기준, 중요도, 근접형 },   ← 길다고 좋은 게 아닌 항목
}
수락 = Σ(충족도 × 중요도) / Σ(중요도) ≥ 임계값
```

- 거절 시 **부족한 항목을 이유로 알려주고** 조건을 고쳐 재협상한다.
- 후보를 늘릴 땐 같은 스키마에 값만 채운다 — 판정 로직은 안 건드린다 (§0.2).
- 오프닝 초기 3인은 난이도와 성향을 각각 대표한다:
  **오성**(쉬움/무관심) · **채아**(중간/성과중시, 뱃지 보너스에 반응) · **블루**(까다로움/평판중시).

### 5.4 스폰서 계약 (기존, 재확인)

- 기한부 조건 계약. 예: "기한 내 뱃지 3개급 트레이너 3명 배출", 미달성 시 해지.
- 평판 오르면 스폰서 제안이 옴. 잠재력 높은 유망주도 평판 따라 알아서 찾아옴.
- **모기업 기대치(§3.3)도 이 구조를 그대로 재사용.**

### 5.5 콘테스트/포켓우드 (서브, 우선순위 낮음)

- 스타성(§4.4) 기반. 소속사가 비용 부담해 콘테스트 참가 → 고평가 시 평판 상승 + 포켓우드(CF) 계약 제안.
- 배틀 트랙과 별개의 성공 경로 — 배틀 약해도 스타성으로 우회 가능.
- 라디오타워는 이 트랙의 CF/미디어 계약처.

### 5.6 연간 시즌 사이클 (NEW, 중요 — 전체 시스템의 시간축)

**시즌은 연 1회 진행되며, 지금까지 설계한 대부분의 시스템이 이 사이클 위의 특정 시기에 배정된다.**

```
시즌 전반부
  → §3.10 오프닝(최초 1회만) / 뱃지 수집(유스, §3.10) / 로컬컵(§5.2)

시즌 후반부
  → 지방 리그: 뱃지 8개를 채운 트레이너가 사천왕·챔피언에 도전 (§5.1, §5.2)
  → 챔피언·사천왕 확정 (또는 로테이션, §5.1)

글로벌 월즈
  → 지방 리그 성적 기반 국제대회, 챔피언은 시드로 자동 진출 (§5.2)

스토브리그 (오프시즌)
  → §5.3 계약 갱신·바이아웃, §3.5 NPC 스카웃/방출, §6 코치 영입/해임
  → 이 시기에 "매니지먼트"라는 장르 정체성이 가장 강하게 드러남

(다음 시즌 반복)
```

- 이 사이클이 확정되면서, 지금까지 따로 놀던 뱃지·리그·계약·스카웃·코치 시스템이 **"언제 벌어지는 일인가"** 기준으로 전부 정렬된다.
- 스토브리그 기간엔 배틀/대회가 없으므로, 이 시기의 UI는 §5.3·§3.5·§6에서 이미 설계한 화면(계약 협상, 시장, 코치 관리)으로 충분 — 새 UI 불필요.

---

## 6. 코치 시스템

### 6.1 코치 보유 여부 = 난이도 축

- **모기업 있는 소속사는 시작부터 코치진이 짜여 있음.**
- **자체 소속사는 코치 없이 시작, 모든 걸 플레이어가 직접 처리.** 이후 성장해서 코치를 계약해야 함 — §3.3 난이도 축에 직접 연결됨.
- **§3.10 오프닝 시퀀스 완료 전까지는 소속사 등급과 무관하게 코치/유스 시스템 자체가 잠겨 있다.** 로스터가 1명뿐이라 코치를 배정할 대상(유스)이 없기 때문 — §0.2 원칙에 따라 로직은 잠그고 스키마만 존재.
- **코치는 소속사당 여러 명 보유 가능** (한 명 한정 아님, 오프닝 이후). 코치 수 자체가 소속사 스케일을 드러내는 지표.

### 6.2 코치 4종 (확정)

| 코치 | 능력치 | 효과 | 대가 |
|---|---|---|---|
| **전력분석관** | 없음(파견 여부만) | 파견 후 복귀하면 상대 에이스 포켓몬 + 그 기술 목록을 획득. §7.2 `perceive()`에서 이 정보에 한해 이번 배틀 한정 노이즈 제거 | 파견 기간 동안 트레이너는 다른 활동 못 함(예: 체육관전 전 파견 보내고 트레이너는 휴식) |
| **포켓몬 훈련관** | 없음 | 포켓몬 개체값·성격 등 세부 육성을 대행 (플레이어의 수동 노가다 대체) | 오래 걸림(구체 기간 §16 미정) |
| **멘토** | **있음** — 유일하게 능력치를 가진 코치 | §4.3 트레이너 5스탯 성장 배수에 직접 작동 | 능력치 낮으면 성장 느림 |
| **보급관** | 없음 | 아이템·기술머신 구매, 특정 포켓몬 포획 파견(§6.3 도로 UI 재사용, 타지방도 가능) | 이동경비·숙식비 차감, 며칠 뒤 복귀 |

**설계 근거 (중요):**
- 세 코치(분석관/훈련관/보급관)는 성격이 같다 — **"파견 나가서 뭔가 가져오는" 역할.** 능력치 스케일링 없이 "쓰느냐 마느냐"의 이진 선택으로 충분하다.
- 멘토만 능력치가 있는 이유: **트레이너의 5스탯(판단력·운영·집중력·지식·멘탈, §4.1)은 배틀 엔진 전체를 좌우하는 핵심 자원**이라, 다른 셋(파견 결과물)보다 무게감이 다르다. 멘토를 "특별히 귀한 코치"로 취급할 근거가 된다.
- v2에서 고려했던 "코치 능력치 4개 축" 및 "3단계 정보량" 설계는 기각됨. FM 정찰 리포트가 "이 선수 조심해라" 한 명 + 장단점 정도만 주는 것을 참고해, 전력분석관 효과는 **"상대 에이스+기술" 고정 정보로 단순화**했다.

### 6.3 파견 UI (분석관 정찰 + 보급관 채집 공용)

- **도로별 UI**: 도로마다 등장 포켓몬 종류 + 레벨 범위를 노출 → "코치 파견" 버튼. 원작 도감 인카운터 테이블을 그대로 UI로 재사용(§0.1-1 에셋 절약 원칙에 부합, 새 UI 불필요).
- 타지방 도로도 같은 화면에서 지방 선택 → 도로 선택만 추가하면 됨. 예: 호연에서 못 잡는 포켓몬을 보급관을 가라르로 보내 잡아옴.
- 전력분석관의 "상대 정찰"도 같은 파견 UI 패턴을 재사용하되, 대상이 도로가 아니라 다음 대전 상대(체육관/리그전 등)라는 점만 다름.
- **파견 중인 코치는 담당 유스 훈련이 멈춤** — "지금 파견 보낼까 계속 육성시킬까"라는 실제 트레이드오프.
- 결과는 §9 뉴스탭에 요약 노출 ("전력분석관이 복귀했다: ○○체육관 에이스는 ??? / 스케일샷 보유").
- **§3.10 오프닝 시퀀스의 "체육관 찾아가기" 맵 UI와 동일 컴포넌트를 재사용할 것.** 별도로 설계하지 말 것.

### 6.4 미정 사항

- 파견 소요 기간(근거리 도로 vs 타지방 왕복 며칠)
- 포켓몬 훈련관의 육성 소요 기간 구체값

---

## 7. 배틀 시스템

### 7.1 재현 범위 (원작 수준 목표)

난수(0.85~1.00), 급소, 명중 실패, 랭크업/다운(−6~+6), 다단히트, 물리/특수 분리, 특성, 도구, 상태이상, 필드/날씨, 우선도. `@pkmn/sim`이 이미 구현되어 있으므로 직접 짜지 말 것. **실빌드에서 Intimidate·Outrage·Sandstorm·혼란 등 정상 작동 확인됨.**

### 7.2 AI 레이어

엔진이 "이 수를 두면 결과가 이렇다"를 계산하고, AI 레이어는 **"어떤 수를 둘까"**만 담당. 두 레이어 분리 유지.

```
공격기 점수 = 상성뺀_기대데미지% × 체감상성 × 스타일가중치
              (기대데미지 ≥ 상대 남은HP% 이면 KO 보너스 +42)

기점기 점수 = (배율−1) × 최대타 × 남은턴 − 이번턴_손해
              배율 = 랭크(현재+상승) / 랭크(현재)
              남은턴 = min(상대 잡는 턴, 내가 버티는 턴, 6)

교체 점수 = (위험도 − 임계값) × 30 + (현재피해 − 벤치피해) × 0.42
            위험도 = 예상피해% / 내 남은HP%
            임계값 = 0.85 + (20 − curve(운영)) × 0.06
```

**실패했던 접근 (반복 금지):**
- 기점기 점수를 1턴 탐욕 점수로 짜면 절대 선택 안 됨 — 반드시 다턴 기대이득으로 계산.
- 지식 스탯을 "상성 적용된 데미지 × 체감상성"으로 짜면 무효기 오판이 구조적으로 불가능 — 반드시 **상성을 뺀 맨몸 데미지**를 먼저 구하고 체감상성을 곱해야 함.

**실빌드 관전 로그에서 발견된 교체 판단 버그 (중요, 확인 필요):**
- 상대가 직전에 랭크업(칼춤 등)을 쓰거나 락인 기술(아웃레이지 등) 사용 중인 상황에서, 그 위협도를 반영 못 하고 부적절한 교체를 하는 사례가 관측됨 (예: 상대가 +2 공격 찍은 직후 물리 방어가 매우 낮은 포켓몬을 자발적으로 투입 → 즉사).
- **의심 지점:** 벤치 후보 평가(`avgDmg` 류 함수) 시 상대방의 **최신 랭크업 상태(boosts)를 반영해 스탯을 재계산하고 있는지** 확인할 것. `@pkmn/sim` 사용 시 `pokemon.getStat()` 계열 API의 boost 반영 옵션이 꺼진 채 기본 스탯만 참조하고 있을 가능성이 높음.
- 강제 교체(기절로 인한)는 정상 작동 확인됨 — 문제는 **자발적 교체 판단**에 한정됨.

### 7.3 표시 방식

| 배틀 등급 | 처리 |
|---|---|
| 일반 배틀 | 자동 스킵 + 텍스트 로그만 |
| 체육관 / 리그 / 대회 결승 | 배속 가능한 관전모드 (스킵 가능) |

시뮬 엔진은 하나. 턴별 이벤트 배열을 뱉고 표시 레이어에서 분기 (§0.1-3 원칙).

**이벤트 스키마 & 로그 포맷:** 프로토타입(`trainer_sim_v2.html`)에서 검증된 구조를 그대로 채택. 포켓몬쇼다운 리플레이 스타일 텍스트 요약. **실빌드에서 동일 포맷으로 정상 출력 확인됨.**

### 7.4 프로토타입 검증 결과 (요약 — 상세는 §14)

- 판단력·지식은 확실히 작동 (스탯 7→20으로 승률 36.8%→84.5%).
- **기본 배틀이 너무 짧으면(4턴대) 기점기·집중력·멘탈이 발동할 틈이 없음.** 목표: 평균 8~12턴. 노력치·도구·6마리 파티로 자연스럽게 도달시킬 것. **실빌드에 "내구 투자(252 HP)"/"6마리 파티" 프리셋으로 이 문제를 검증 중.**
- 물리/특수 판별 실험은 방어 스탯이 비대칭인 포켓몬으로 할 것.

### 7.5 탐색적 실험: 저스탯 트레이너가 사기 포켓몬/저격팀으로 이길 수 있는가

**주의: 이 실험은 임시 테스트 스크립트에서만 진행됐고, `trainer_sim_v2.html` 본 파일에는 반영돼 있지 않다. 코드가 아니라 밸런싱 인사이트로만 참고할 것.**

미라이돈·코라이돈(BST 670급) 같은 압도적 종족값 포켓몬을 저스탯 트레이너(전 스탯 8)에게 쥐여줘도, 고스탯 상대(전 스탯 20, 평범한 팀)를 이기지 못했다.

| 매치업 | 승률 |
|---|---|
| 저스탯(전 8) + 일반팀 vs 고스탯(전 20) + 일반팀 | 29.0% (기준선) |
| 저스탯(전 8) + **미라이돈·코라이돈** vs 고스탯(전 20) + 일반팀 | 28.6% (거의 안 변함) |
| 저스탯(전 8) + **타입 저격팀(자시안 크라운 등)** vs 고스탯(전 20) + 일반팀 | **42.4%** (확실히 오름) |
| 스탯 동일(전 8) 조건에서 사기팀 효과 | 60.6% |

**해석:** §7.2의 AI는 "종족값이 세다"가 아니라 "판단력에 따라 최적수를 얼마나 잘 찾는가"로 작동하므로, 판단력이 낮으면 강한 포켓몬도 제대로 못 쓴다. 반대로 **타입 상성 저격은 판단력이 낮아도 유효하다** — 복잡한 최적화 없이 상성 자체가 유리함을 만들어주기 때문. 이건 §5.3 영입 경쟁(자금 없는 신인 소속사가 챔피언급을 영입해도 못 다루면 의미 없음)과 §3.3 "자체" 난이도의 실질 전략 축(저격 픽 발굴)을 뒷받침하는 근거로 쓸 수 있다.

### 7.6 아이템(회복약) 엔진 통합 — 설계 공백 (NEW, 확인 필요)

**`@pkmn/sim`은 경쟁 대전 시뮬레이터라 "배틀 중 가방에서 아이템을 꺼내 쓰는" 개념 자체가 없다.** 이건 이 프로젝트 고유의 매니지먼트 메커니즘(§4.1 운영 스탯의 절반)이라 엔진에 없는 걸 별도로 얹어야 한다. 실빌드에서 미구현 상태로 확인됨.

**설계 방향 제안:** 배틀 턴 사이에 AI 레이어가 개입해, `@pkmn/sim`에 턴을 넘기기 전 "이번 턴 회복약을 쓸지" 먼저 판단 → 쓰기로 하면 엔진 API를 우회해 HP를 직접 조작하는 후처리 필요. `@pkmn/sim` 내부 구조(배틀 상태 직접 조작 가능 여부)를 확인 후 구체적 구현 방식 결정할 것.

---

## 8. 그래픽 & 스프라이트

- **스프라이트는 쇼다운 gen4(HGSS 세대) 도트를 링크해서 쓴다.** 그 세대에 없는 종(신규 세대 포켓몬 등)은 **상위 세대 스프라이트로 자동 대체**된다 — 실빌드에서 이 폴백 처리 확인됨.
- **아티팩트/브라우저 샌드박스 환경에서는 외부 이미지 도메인이 CSP로 차단될 수 있음.** 실제 배포 환경(GitHub Pages 등)에서는 문제 없이 작동 확인됨(실빌드가 GitHub Pages에서 정상 렌더링). 로컬 프로토타입/아티팩트 단계에서만 CSP 이슈 발생 — base64 내장 등으로 우회 가능(§7.4 프로토타입에서 이미 적용).
- 스프라이트는 여전히 원작 저작권물. 별도 라이선스 없음 — §15 리스크 참고.

---

## 9. 뉴스 시스템 (통합 파이프라인)

**대회 결과, NPC 소속사 스카웃/방출, 코치 파견 결과, 전설의 포켓몬 목격담/포획 소식, 유스 뱃지 진행 상황이 전부 하나의 뉴스탭으로 흘러들어간다.** 새 UI를 여러 개 만들지 않고 하나의 파이프라인을 공유하는 게 핵심. **실빌드에서 이 파이프라인 확인됨(소식탭에 대회 우승 소식 정상 노출).**

```
하루 진행(배틀/훈련/파견 등 행동 소비)
  ↓
그날 벌어진 일들을 이벤트로 누적 (§7.3의 턴별 이벤트 패턴을 하루 단위로 재사용)
  ↓
Day 종료 시 뉴스탭에 요약 노출
```

- 경량 계층(§3.4) 대회·NPC 소속사 결과는 텍스트 한 줄 수준으로 충분 ("○○ 대회 우승: 봄", "○○ 소속사가 △△를 영입").
- 무거운 배틀 재시뮬 없이, 참가자 스탯 비교 + 약간의 난수로 승자만 빠르게 뽑는 경량 로직으로 처리.
- **전설의 포켓몬은 2단계 노출**이 필요함 — §11 참고.
- **유스 뱃지 진행 상황은 §3.10에서 정한 노출 리듬(초반 조용, 후반 밀집)을 따를 것.**

---

## 10. 지역 확장 시 등급 판정 기준 (재사용 가능한 규칙)

§3.2에서 확립한 기준을 이후 지역에도 그대로 적용한다.

> **소속사/조직 등급 = 원작에서 실제로 차지한 영향력 범위(지역 수, 점거·점령 이벤트 유무)**

참고용 사전 판단(확정 아님, 실제 스코프 시 재검토):

- **갤럭시단(신오)**: 호수 세 개+창기둥 등 신오 전역 활동 → 로켓단급(세미대기업) 후보
- **플라즈마단(하나)**: 도시 자체를 점거하는 이벤트 있음 → 세미대기업급 후보
- **플레어단(칼로스)**: 거점은 좁지만 궁극병기 하나에 파괴력 집중 → "범위"보다 "파괴력" 별도 축 필요할 수 있음
- **스컬단(알로라)**: 폐건물 하나가 거점 → 중소~영세급

---

## 11. 전설의 포켓몬(전포) 시스템

§0.1-4 "세상이 이미 굴러간다" 원칙을 전설의 포켓몬에도 확장한 것. 스카웃·대회처럼 전포도 플레이어를 기다리지 않는다.

### 11.1 메커니즘

```
전포는 §6.3 도로별 인카운터 테이블에 안 뜨는 별도 카테고리
  → 특정 조건(퀘스트, 목격담 이벤트 등)으로만 위치 노출
  ↓
매 사이클(§9 하루 단위) 아주 낮은 확률로
  정밀 계층(§3.4) NPC 중 하나가 "먼저 포획 시도"를 굴림
  ↓
성공: 그 전포는 시장에서 사라지고 해당 NPC의 실제 파티에 편입
실패: 다음 사이클에 또 낮은 확률로 재시도 (계속 열려있음)
```

- **NPC는 잡은 전포를 실제로 파티에 넣어 사용한다.** 별도의 강함 보정 로직은 없음 — `@pkmn/sim`의 실제 종족값을 그대로 반영하면, 레쿠쟈를 잡은 NPC와 레지기가스를 잡은 NPC의 위협도가 자연스럽게 갈린다. 이 차이를 플레이어가 뉴스탭에서 이름만 보고 직접 판단하게 되는 것도 의도된 재미.

### 11.2 뉴스탭 2단계 노출 (§9 파이프라인 재사용)

```
1단계: "○○지역에서 정체불명의 포켓몬 목격담" (사전 경고, 플레이어에게 서두를 기회)
2단계(며칠 뒤): "△△소속사가 포획에 성공했다" (놓쳤을 때의 서사적 납득)
```

완전 랜덤으로 뺏기는 게 아니라 "기회가 있었는데 안 갔다"는 납득 가능한 서사로 만드는 장치.

### 11.3 NPC가 선점해도 완전히 끝은 아님

§5.3 위약금 구조를 재사용해, NPC가 먼저 잡은 전포도 나중에 그 소속사를 상대로 **트레이드/영입 시도**를 열어둔다. 완전 봉쇄보다 "더 비싸고 어려운 경로로 여전히 노릴 수 있다"가 덜 억울하다.

### 11.4 미정 사항

- 포획 확률이 전 전포에 균일한지, 강한 전포일수록 더 희귀하게(반비례) 할지 (§16)
- 목격담→포획 사이 정확한 유예 기간

---

## 12. NPC 성장/적응 시스템

§5.1에서 네임드는 "스탯 고정"으로 확정했지만, **스탯이 아니라 로드아웃(장비/전술)이 성장하는 건 별개 축**으로 넣을 수 있다. 이 구분이 핵심.

### 12.1 시즌별 로스터 개방

```
네임드 스탯: 고정 (§5.1 그대로 유지, 변경 없음)
네임드 로드아웃: 시즌마다 갱신 가능
  시즌1: 기본 팀
  시즌3: 메가진화 스톤 획득 → 팀에 편입
  시즌5: 신기술/도구 추가
```

- "챔피언이 강해졌다"가 아니라 "챔피언이 새 장비를 갖췄다"는 서사. 계산 비용도 낮음 — 시즌 번호별 로드아웃 테이블 하나면 충분 (§0.2 원칙 부합). §5.6 연간 사이클의 "시즌" 단위와 동일 개념이다.

### 12.2 플레이어 맞춤 견제 (정밀 계층 한정)

§3.4 정밀 계층 소속사에서만 작동. 경량 계층 NPC는 플레이어를 안 만나므로 해당 없음.

```
NPC가 "플레이어 프로필"을 누적:
  최근 배틀에서 플레이어가 자주 쓴 타입/포켓몬 기록
  ↓
플레이어와 재대결 앞두고 카운터 준비:
  플레이어가 드래곤 자주 씀 → NPC가 페어리/얼음 커버리지 추가
  플레이어가 특정 에이스를 자주 씀 → 그 카운터 픽 준비
```

- §7.2 `perceive()` 로직을 **NPC 쪽에 뒤집어 적용**한 것 — 새 메커니즘 없이 기존 함수 재사용.
- **트리거는 플레이어 평판 임계값** — §5.4 평판 시스템과 연동. 유명해질수록 상대도 진지해진다는 자연스러운 난이도 곡선.
- **완벽한 카운터는 금지.** 최근 N번 배틀만 참고하거나 일정 확률로만 반영하는 식으로 노이즈를 둬서, 플레이어가 대응할 여지를 남길 것. 정확한 노이즈 수준은 §16 미정.

### 12.3 UI 주의사항

플레이어가 "이게 원래 강한 건지, 나를 연구해서 강해진 건지" 구분 못 하면 혼란스럽다. **뉴스탭에 "○○이(가) 당신의 최근 전적을 분석했다" 같은 한 줄을 반드시 넣어서, 시즌 개방(§12.1)과 맞춤 견제(§12.2)를 플레이어가 구분할 수 있게 할 것.**

---

## 13. 구현 우선순위

**§0.2 원칙에 따라, 아래 순서는 "기능 구현 순서"이지 "스키마 설계 순서"가 아니다. 스키마는 1단계 착수 전에 이 문서 전체를 반영해 먼저 설계할 것.**

**진행 상황(실빌드 기준): 1~3단계는 상당 부분 진행됨(배틀 엔진 통합·기본 리그 UI·대회 로직 동작 확인). 현재 발견된 버그(§7.2 교체 판단, §7.6 아이템 공백) 수정과 §3.10 오프닝 시퀀스가 다음 우선순위.**

| 단계 | 내용 | 비고 |
|---|---|---|
| 1 | `@pkmn/sim` 세팅 + 번들링 | **완료 확인** |
| 2 | AI 레이어 이식 (판단력·운영, 곡선 포함) | **완료 확인**, 단 §7.2 교체 판단 버그 수정 필요 |
| 3 | 팀 빌딩 + 배틀 길이 튜닝 (§7.4) | 진행 중 (프리셋 존재) |
| 3.5 | **대회 브라켓 진출 버그 수정** | 관전 후 패자가 다음 라운드로 올라가는 버그 발견, 기존 대회 결과 오염 여부 확인 필요 |
| 3.6 | **오프닝 시퀀스 (§3.10)** | 로스터 1명 시작, 체육관 8개 도전, 맵 UI, 로스터 확장/유스 개방 이벤트 |
| 4 | 집중력·지식·멘탈·연패 페널티 검증 | 배틀이 길어져야 검증 가능 |
| 5 | 천성(순응도·스타일) 연결 | |
| 6 | 육성 루프 (PA/CA, 훈련) | |
| 7 | 소속사 시스템(§3) 나머지 — 정밀 계층 | 오프닝 이후 개방 |
| 8 | 코치 시스템 (§6) — 4종 | 멘토만 능력치 있음에 유의, 오프닝 이후 개방 |
| 8.5 | **아이템(회복약) 엔진 통합 (§7.6)** | `@pkmn/sim` API 조사 후 설계 확정 |
| 9 | 계약/바이아웃/경질 (§5.1, §5.3) | |
| 10 | 대회 시스템(§5.2, 인원 제한 포함) + 뉴스 파이프라인(§9) | **뉴스 파이프라인 기본형 완료 확인** |
| 10.5 | 지방 리그·사천왕·챔피언 로테이션 (§5.1, §5.6) | |
| 11 | 경량 계층 NPC 소속사 (타지방) | |
| 12 | 스카웃·창고·유스 (§6.3, 기존 창고 시스템) | |
| 13 | 전설의 포켓몬(§11) | NPC 선점 로직 + 2단계 뉴스 |
| 14 | NPC 성장/적응(§12) | 정밀 계층 완성 후 |
| 15 | 콘테스트·포켓우드 (§5.5) | 서브, 최후순위 |

---

## 14. 프로토타입 검증 결과 (상세)

동봉 `trainer_sim_v2.html`. 테스트 매치업: 난천(화강돌·한카리아스) vs 단델(드래펄트·리자몽), Lv50/노력치0/무보정/개체값31.

### ✅ 검증된 것
- 판단력·지식 확실히 작동 (§7.4 요약 참고)
- 특성·랭크·다단히트·물리특수 분리 정상 동작
- 스탯 비선형 곡선(§4.2)으로 네임드 분리 확보
- (탐색적) 저스탯+사기 포켓몬은 안 통하고, 저스탯+타입 저격은 통함 — §7.5
- **실빌드에서 스타일 가중치(기점형=상태변화기 가중치 1.8배) 정상 작동 확인** — 기점형끼리 매치업하면 양측 다 초반 칼춤을 선택하는 것으로 검증됨

### ⚠️ 발견된 문제 (해결 전 재현 금지)
1. **배틀이 너무 짧으면 스탯 절반이 놀고 있음.** 평균 4.4턴대에선 기점기 0% 선택, 집중력·멘탈 발동 불가. 목표 8~12턴 확보 전엔 이 스탯들 검증 불가.
2. **내구를 올리면 회복약이 깨짐.** 배틀 길이 조정 시 회복약 개수·회복량 동시 재조정 필요.
3. **물리/특수 판별 실험은 방어 스탯이 대칭인 포켓몬으로 하면 판별력 없음.**
4. **저주처럼 자기 HP를 대가로 쓰는 기술은 최대HP 낮은 포켓몬에게 부적합** — AI가 합리적으로 회피함.
5. **새 포켓몬/기술을 테스트 스크립트에 추가할 때, `MV` 테이블에 그 기술이 실제로 정의돼 있는지 항상 먼저 확인할 것** — §7.5 실험 중 미정의 기술(`오버히트`, `명상`)을 참조해 `undefined` 크래시가 났던 적 있음. 본 파일 버그는 아니었으나 반복될 수 있는 실수 패턴.
6. **[실빌드] 대회 브라켓 진출 로직 버그**: 관전 후 패자가 다음 라운드에 진출하는 현상 관측됨. 승자/패자 변수 대입이 뒤바뀌었을 가능성이 높음. 이미 진행된 대회의 순위표·레이팅·뉴스탭 기록이 이 버그로 오염됐을 수 있으니, 수정 후 기존 데이터 재검증 또는 리셋 여부를 판단할 것.
7. **[실빌드] 자발적 교체 판단 버그**: 상대의 최신 랭크업/락인 상태를 반영 못 하고 부적절한 교체를 하는 사례. §7.2 참고.
8. **[실빌드] 아이템(회복약) 미구현**: `@pkmn/sim`에 트레이너 가방 개념이 없어 별도 설계 필요. §7.6 참고.

---

## 15. 리스크

**포켓몬 IP 팬게임이라 상업화 불가이며, 저작권자가 배포 중단을 요구할 수 있다.**

- `@pkmn/sim`은 MIT라 엔진 자체는 문제없으나, 포켓몬 이름·스프라이트·기술명·지역/조직 설정(실프·로켓단 등)은 전부 별개의 저작권 문제.
- **실프·데본 등 실존 원작 기업을 "스케일 기준점"으로만 쓰고 직접 조작 가능한 캐릭터로 만들지 않은 것**(§3.1)은 리스크를 줄이는 선택이지만, 이름 자체를 쓰는 이상 리스크가 사라지진 않는다.
- 포트폴리오·홍보 목적이라면 이 시스템 자체(트레이너 육성 매니지먼트 코어 루프)는 포켓몬이 아니어도 성립한다는 점을 계속 염두에 둘 것 — 오리지널 IP로 껍데기만 바꾸면 상업화·외부 승인이 모두 가능해진다.

### 15.1 경쟁작 조사 결과

일반 웹검색 + 팬게임 커뮤니티(PokéCommunity) 검색 기준.

| 프로젝트 | 겹치는 부분 | 안 겹치는 부분 |
|---|---|---|
| **Gym Manager 2023** (PokéCommunity, RPG Maker) | 체육관 트레이너 로스터를 매일 관리, 아이템 구매로 강화, 포켓몬 "입양" | 체육관 하나 스코프. AI가 스탯에 따라 실수하는 메커니즘 없음. 다중 소속사 경쟁·계약·코치 없음 |
| **소원의별** (국산 팬게임, 포켓몬 에센셜 기반) | 플레이어가 체육관 관장(도전자 아님) 시점 | 1인칭 직접 배틀. 소속사·다중 트레이너 관리·계약 없음 |
| **체육관 타이쿤** (유튜브 확인, 국산) | "타이쿤+포켓몬" 발상 자체 | **완성 후 제작자가 직접 폐기함.** 실제 시스템 불명 |
| "Pokémon League Manager" (Siliconera 기사, 2021) | 리그 매니저로서 관장을 스카웃/배치하는 컨셉 | 실제로 만들어진 적 없는 아이디어 제안 글 |
| "Pokémon League!" (Football Manager 모드) | 관동/성도 배경, 로켓단을 팀으로 등장시킴 | 실제 축구 경기를 함. 포켓몬 배틀 없음 |

**결론:** "다중 소속사가 AI 판단 기반으로 경쟁하는 트레이너 매니지먼트"라는 정확한 조합은 검색상 확인되지 않음. 다만 개별 요소(관장 시점, 타이쿤 경영, 로스터 관리)는 각각 시도된 바 있고, 특히 체육관 타이쿤이 완성 후 폐기됐다는 점은 이 장르 자체의 완성 난도가 낮지 않다는 신호로 참고할 것.

### 15.2 배포 플랫폼 정책 리스크 (실무적으로 중요)

**PokéCommunity 공식 규정: 생성형 AI로 만든 콘텐츠는 게임 어느 부분(타이틀 화면, 아트워크, 게임 요약 등)이든 게시 금지.** 이 프로젝트는 클로드 코드로 개발되므로, PokéCommunity에 배포할 계획이면 이 규정이 직접 걸림돌이 된다. Relic Castle 등 다른 팬게임 커뮤니티는 정책이 다를 수 있으므로, 배포처를 정할 때 반드시 사전 확인할 것.

---

## 16. 미결정 사항 (v4 기준)

- 신오 리그 대기업 자리 (§3.8)
- 후임 네임드 대체 캐릭터의 파티 처리(원래 파티 계승 vs 신규 생성) (§5.1)
- 메이저 대회 소속사별 출전 인원 제한 정확한 수 (2명? 3명? 등급별 차등?) (§3.9, §5.2)
- 코치 파견 소요 기간(분석관/보급관), 포켓몬 훈련관 육성 소요 기간 (§6.4)
- 유망주 유입 방식(스카웃 화면 후보 노출 vs 자동 합류)
- 스폰서 다중 계약 허용 여부
- 콘테스트 평가 기준(스타성 단독 vs 원작 5카테고리)
- 대회 출전 자격의 정확한 필터 조합 (§5.2)
- 전포 포획 확률 — 전체 균일 vs 전포 세기 반비례 (§11.4)
- 전포 목격담→포획 사이 유예 기간 (§11.4)
- NPC 플레이어 맞춤 견제의 노이즈/정확도 수준 (§12.2)
- 배포 플랫폼 선정 (PokéCommunity의 AI 콘텐츠 금지 규정 확인 필요, §15.2)
- 아이템(회복약) 엔진 통합 구체 방식 (§7.6)
- 관동·성도 16명 체육관 관장의 정확한 원작 데이터 전수 확정 (§3.10 — 예시로 검증된 원규 외 나머지)


### 2026-09-10 플레이 화면 수정 지시

- 기존 대회 시스템은 현재 플레이에서 제거한다. 자동 개최·참가·대회 일정·리그 순위 메뉴 없음. 설계 재정의 전까지 엔진의 대회 데이터 구조는 호환용으로만 보존한다.
- 대표실 / 받은 메시지함 / 스쿼드 / 포켓몬 박스 / 탐험 지도 / 활동 일정 / 스카우팅으로 구성.
- 하루 진행은 실제 활동 → 소속사 기록 → 정산 → 보고서 단계. 별도 훈련은 복원하지 않는다.
- 지방 지도는 공통 SVG 도식. 관동·성도 67개 장소만 실제 탐험, 나머지 지방은 미리보기라고 표시한다.
- 달력 시작일은 3월 1일, 주급은 7일마다 지급하는 임시 기본값. 연간 시즌 규칙은 아직 구현하지 않음.
