# 진행 상황 — 플레이 가능한 하루 단위 매니지먼트로 전면 수정

> 이 파일만 보면 뭘 끝냈고 다음에 뭘 해야 하는지 알 수 있게 유지한다.

## 방향 전환 (중요)

이전 세션 결과물은 **"리그가 알아서 돌아가는 걸 구경하는 시뮬"**이었고, 플레이어가 하는 게 없었다.
FM의 핵심인 *"내가 매일 굴린다"*는 감각이 전혀 없다는 지적을 받아 전면 재작업한다.

**확정된 범위 (사용자 확인)**
- 배틀 시뮬 탭은 **삭제**. 게임 화면만 남긴다.
- 조작 범위: 지시/엔트리 + **트레이너별 일일 일정 직접 배정** + **스카웃/영입·방출**
- **성장(PA/CA) 포함** — 하루하루가 쌓이는 감각의 핵심 (이전 세션의 "육성 제외" 방침 철회)

---

## 목표 게임 루프

```
[하루]
  1. 오늘 상황 확인 (자금 / 트레이너 컨디션 / 예정된 대회 / 시장)
  2. 트레이너별로 오늘 할 일 배정 — 훈련(스탯 지정) / 휴식 / 대회 출전
  3. 필요하면 시장에서 영입·방출 (자금 소모, 일일 경비 증가)
  4. [다음 날] -> 행동 실행 · 대회 개최 · 경비 차감 · NPC 세계도 같이 진행
  5. 결과 확인 + 내 트레이너 배틀 관전
```

**결정이 재미있어지는 지점 (핵심 트레이드오프)**
- 대회 출전은 컨디션을 크게 깎는다 -> 연속 출전 불가 -> "누구를 언제 내보낼까"
- 훈련시키면 강해지지만 **그날 대회에 못 나간다** -> 단기 상금 vs 장기 성장
- 일일 경비가 계속 나간다 -> 안 나가면 말라죽음 -> 무리해서라도 벌어야 함
- 영입은 즉시 전력이지만 일일 경비가 늘어난다

---

## 상태 요약

| # | 항목 | 상태 |
|---|---|---|
| 1 | 게임 상태 모델 + 성장(PA/CA) + 컨디션 | 완료 |
| 2 | 하루 진행 엔진 (행동 실행 / 대회 / 경비 / NPC) | 완료 |
| 3 | 스카웃 시장 (영입·방출) | 완료 |
| 4 | UI 전면 교체 (배틀 시뮬 탭 삭제) | 완료 |
| 5 | 밸런스 확인 (자금 압박·성장 체감) | 완료 (1차) |

---

## 구현 결과

**새 파일**
- `src/engine/game.js` — 게임의 심장. `advanceDay()` 하나가 하루를 처리한다:
  배정 행동 실행 -> 대회 개최(내 엔트리 + NPC 자동 판단) -> 컨디션 소모 -> 성장 -> 경비 -> 뉴스 -> 파산 판정
- `src/ui/game-view.js` — 6개 탭 (오늘 / 트레이너 / 대회 / 시장 / 리그 / 소식)

**바뀐 파일**
- `src/data/agencies.js` — 트레이너에 `potential`(PA, 숨김) / `condition` / `salary` 추가.
  `effectiveStats()` — 컨디션이 배틀 능력에 실제로 곱해진다.
- `src/engine/league.js` — 컨디션 반영 배틀, 플레이어 소속사 제외 옵션, `makeTrainerFor()`(시장 공용)
- `index.html` — 전면 재작성 (CSS는 그대로 살림). 배틀 시뮬 탭 삭제.
- `src/main.js` — 진입점만 남김. `src/ui/league-view.js` 삭제(게임 UI에 흡수).

**게임이 실제로 성립하는지 확인한 것 (UI에서 30일 플레이)**
- 훈련하면 스탯이 오른다 (판단력 +0.48/일 같은 식, 잠재력에 가까울수록 느려짐)
- 대회 출전은 컨디션 -30 -> 연속 출전 불가 -> 로테이션이 강제된다
- 출전 자격이 레이팅 밴드로 걸린다 (레이팅 16.5인 조현우는 하급 컵 출전 불가)
- 자금: 34일차에 3,000 -> 2,242 (영입 1명 포함). 평판 20 -> 74, 우승 3회.
  **적당히 굴리면 살아남고 못 굴리면 마른다** — 압박이 실제로 존재
- 영입하면 일일 경비가 즉시 오른다 (60 -> 73)
- 내 트레이너 경기는 저장된 시드로 그대로 재생해서 관전 가능

---

## 다음에 이어서 할 때

**바로 붙일 수 있는 것**
- 만족도 시스템: 필드(`satisfaction`)와 성장 계수 연결은 이미 되어 있음. 출전 기회/성적에
  따라 오르내리게만 하면 §5.3 바이아웃·이탈로 이어짐
- 시즌/승격: 지금은 날짜가 무한히 흐르기만 한다. 시즌 종료 -> 모기업 기대치 평가(§3.3)
- 코치(§6): `trainer.coachAssigned`, `agency.coaches` 필드 준비됨
- 천성 연결(§4.4): `nature.style`이 이미 AI에 전달되고 있음. 데이터만 다양화하면 작동

**알려진 한계 / 판단 필요**
- Q. 파산 외에 목표가 없다. 시즌 목표(예: "1시즌 안에 평판 100")가 있어야 방향이 생김
- Q. NPC 소속사는 영입/방출을 안 한다 (성장만 함). §3.4 경량 로직을 붙일지
- Q. 밸런스는 1차만 맞춘 상태. 실제로 오래 플레이해보고 조정 필요
- 회복약(가방 아이템) 여전히 미구현

---

## 재사용 / 폐기

**그대로 재사용** — 이전 세션이 헛일은 아님
- 배틀 엔진 + 트레이너 AI (`ai/`, `engine/run-battle.js`) — 검증 끝난 부분
- 관전 화면 (`ui/battle-view.js`, `ui/protocol-ko.js`, 스프라이트) — 내 트레이너 배틀 볼 때 그대로 씀
- 포켓몬 풀 · 팀 조립기 (`data/pokemon-pool.js`, `engine/team-builder.js`)
- 소속사/트레이너 스키마 (`data/agencies.js`) — §0.2대로 필드를 다 만들어둔 게 여기서 회수됨
- 대회 등급·참가 판단 (`engine/league.js`의 §3.5 로직) — NPC 소속사용으로 계속 씀

**폐기**
- 배틀 시뮬 탭 UI (`src/main.js`의 슬라이더/프리셋/200회 시뮬)
- "시즌 끝까지 한 번에 돌리기" — 구경하는 물건이라 방향과 반대

---

## 이번에도 제외 (스키마만 유지)

- 코치 시스템 (§6) — 파견/멘토
- 계약 협상·바이아웃 (§5.3) — 영입은 단순 이적료로만
- 글로벌 대회 인원 제한 (§5.2), 경량 계층 타지방 (§3.4)
- 전설의 포켓몬 (§11), NPC 성장/적응 (§12), 콘테스트 (§5.5)
- 회복약(가방 아이템) — @pkmn/sim에 없는 개념
