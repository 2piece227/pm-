# 진행 상황 — 관동·성도 매니지먼트

## 작업 18 — 후보 선제안 / 가중 협상 / 계약 후 다음 (완료)

- 후보를 고르면 자신의 선호 조건을 먼저 말하고 입력칸도 그 제안으로 시작. 평판이 부족하면 자신이 중시하는 보상을 더 요구한다. 검색/채팅/거절 후 수정 흐름 유지.
- 모든 후보가 동일한 선호 스키마/판정식을 사용. 중요도 제곱 가중치, 부족분의 충족도 제곱, 초과 보상은 1+log2(배율), 최대4배 효용. 수락 기준0.92. 하드 주급 컷 없음. 관심0 항목은 보상 효용도0이므로 후보 개성 유지.
- 기본값 질문: 가중 지수2/수락0.92/효용상한4와 개런티 최대20000은 임시 밸런스. 장기 총 보장액/계약 기간별 금전 할인은 아직 반영하지 않는다.
- 직접 확인: 블루 선제안 주급20/개런티400/승급150% 인상/5년 → 주급5로 수정시 거절 → 개런티4000으로만 올리면 수락, 실제 자금20000→16000/주급5. 제안 직전 입력을 읽어 이전 수치로 계약되는 문제 수정.
- 계약 즉시 로스터/개런티/계약 조건을 저장하고 완료 화면에서 멈춤. 다음 버튼을 눌러야 스타팅 선택. 선택 전 하루 진행 금지. 재접속해도 완료 화면 복구, 재과금/중복 스타팅 방지.
- 직접 확인: 계약 완료 상태에서 새 탭으로 이어하기 → 동일 자금/계약/파트너 대기 → 다음 → 이상해씨 선택/확정 → 지도 진입. 기존 저장 호환.
- 추가 점검 수정: 승급150% 표현을 2.5배 급여와 일치시킴. 첫 트레이너 이후에 영입한 유스도 자기 지역8배지 달성 시 계약 인상률을 한 번 적용. 박스 이동은 자기 소속사 트레이너만 허용.
- 화면 검수: 도트 포켓몬·4칸 기술 UI·어두운 검색칸 확인, 장착 기술의 비활성 글자 대비 보완.
- 자동 검사: contract-flow (후보 선제안 수락, 가중치 차이, 낮은 주급/높은 개런티, 자금/잘못된 값, 단계 저장/복원, 중복 방지), pokemon-management, management-flow 통과. 상세 부족점은 REVIEW_2026-09-10.md.

## 작업 17 — 포켓몬 관리 / 체육관 / 지원 / 장기 훈련 (완료)

- 요청 범위: 지도 클릭점 확대, 트레이너 명칭, 4칸 기술 UI와 유료 변경/특성 변경, 레벨 진화, 16개 체육관과 자동 관전, 진심 파티 데이터, 유한 물품 지급, 박스 IV 장기 훈련, 표정 피로도, 최대 8활동.
- 원작 기준: 관동은 FRLG 첫 도전(비주기 포함), 성도는 HGSS 첫 도전. 버전과 포켓몬/레벨/기술을 공개 게임 데이터에서 확인. 진심 파티는 HGSS 재대결이 있는 관장은 해당 데이터 사용; 다른 관장은 출처/임시 구성을 구별.
- 순서: 관리/진화/지원/훈련 엔진 → 관장 데이터/뱃지/관전 → UI 연결 → 저장·회귀 검사/직접 플레이.
- 기본값 질문: 기술 슬롯 변경 100, 특성 변경 1000; 몬스터볼 200·상처약 300. IV 1포인트당 2일(26 이상은 3일), 파티에서 빠진 박스 개체만 훈련. 가격/기간은 설정으로 분리하고 변경 가능하게 둠.

- 구현 완료: 도로 클릭 반경 확대, 스쿼드→트레이너, 4칸 기술 선택과 상세/확정, 유료 특성 변경, 수동·자동 레벨 진화. 돌/교환/친밀도 등 특수 진화는 조건 표시만 하고 별도 로직은 다음 범위.
- 구현 완료: 유한 가방, 수량별 구매·지급, 상처약 HP20/활동1칸, 볼 소진 시 포획 중단(병행이면 배틀). 기존 저장에는 무료 볼을 추가하지 않음.
- 구현 완료: 박스 IV 목표 설정/기간 예고/일별 진행/취소/파티 잠금/저장 복원. 각 IV1당2일, 목표값26부터3일. 완료한 포인트만 적용.
- 구현 완료: 16체육관 첫 도전, 자동 관전/일시정지/배속/다시보기. 경기 로그를 저장해 관전에서 재계산하지 않음. 같은 배지/상금 재지급 방지, 한 지방8배지로 추가 계약 개방 및 승급 급여 적용.
- 진심 파티: 15명 HGSS 상위 재대결(독수는 사천왕), 비주기는 임시 자체6마리+기술 구성. PWT 기능은 아직 없음.
- 적용 범위: 관장 종족/레벨/기술/아이템은 원작 자료 기준. 전투는 기존 Gen9 규칙/시드 IV/고정 성격·AI이므로 원작 전체 배틀 재현이라고 주장하지 않음. 관장전 중 가방약은 미구현, 탐험 사이 사용만 구현.
- 자동 검사 통과: pokemon-management (과금 원자성, 진화, 관장 승리 후 자동진화/패배, 볼/약, 피로, IV 장기훈련·잠금·저장, 16관장, 배지 중복 방지/8개 개방), exploration-day(6/7/8), management-flow, 배틀 재생52경기/기존 브래킷52경기. 기존 대회 로직은 정상 플레이에서 계속 비활성.
- 직접 UI 확인: 기존 저장 이어하기 → 파이리 특성 변경1000 → 볼3개600·약1개300 지급 → 구구 공격IV15→16에2일 배정 → 웅 도전 자동 관전/일시정지/배속/패배 확인 → 다음 휴식일에 IV16 훈련 완료. 관전은 꼬마돌Lv12·롱스톤Lv14 파티를 사용.
- 최종 빌드/저장 검증 통과. 작업18과 함께 커밋하며 배포 결과는 GitHub Actions에서 확인.

## 작업 16 — 하루 탐험 확장 / 커뮤니티 도트 타운맵 (완료, 2026-09-10)

- 원인: 기존 explore는 하루 한 판이고 센터는 문장만 출력. 배틀마다 HP/상태/PP가 초기화되던 구조를 수정.
- 완료: 하루 6~10활동(기본 8) 선택, 포획 중심/배틀 중심/병행. 센터 방문도 1칸 소비하며 회복 후 남은 활동을 이어감.
- 완료: 실제 배틀 사이와 날짜 사이에 HP/상태/PP 유지. 쓰러진 개체/상태이상/HP 50% 미만/장착 기술 전체 PP 소진 시 센터. 예산 밖 자동 회복 없음. 하루 휴식은 포켓몬 완전 회복과 멘탈에 따른 자신감 회복.
- 완료: 다중 포획을 빠짐없이 박스 입고, 상금 합산, 경험치·레벨·기술 습득·센터·활동 시각·전후 체력을 시간순 보고서에 표시. 반복 NPC는 재대결이라고 안내.
- 추가 수정: Showdown 교체 시 파티 배열 순서가 바뀌어 다른 개체 HP가 저장될 가능성을 원래 슬롯 참조로 차단. 기술을 빼고 다시 장착해도 소모 PP를 복구할 수 없도록 보존.
- 지도: Eevee Expo ENLS의 Bill's Experiments Episode 1 Resource Pack에서 HGSS 기반 mapRegion0.png 확보. 816×322 원본을 로컬 에셋으로 포함, 픽셀 렌더링, 관동/성도별 표시 영역과 67개 장소 클릭 좌표 적용. 이전 SVG 지형 제거. 호연~팔데아는 미개방으로 표시하며 임시 그림을 만들지 않음.
- 출처: CREDITS.md와 public/assets/maps/README.md에 제작자/원작 권리/다운로드 주소/팩 크레딧 명시. 표준 CC/오픈소스 라이선스라고 주장하지 않음.
- 화면 다듬기: 진행 창에서 각 활동 완료와 진행률 표시. 편지함의 중복 보고서 제거, 짧은 미리보기와 선택한 날짜의 상세 활동 기록. 대표실 기록은 접어서 표시. 맵/스쿼드에 실제 남은 HP 노출.
- 자동 검사 통과: exploration-day (6/8/10칸, 센터 포함, HP/PP/상태 지속, 다중 포획, 상금, 멘탈, 저장 복원, 3연패와 센터 교대, 기술 교체 PP 보존, 교체 후 슬롯, 67좌표), management-flow, 기존 배틀 일관성 52경기/브래킷 52경기. 대회는 정상 플레이에서 계속 제거 상태.
- 실제 브라우저: 기존 3일차 저장 이어하기 → 1번도로 10회 병행 → 9승/포획4/센터1/상금192, 파이리 Lv6→9 및 꼬렛 Lv5→8. 재접속 후 4일차/박스4/파티HP28/50 유지. 관동·성도 도트맵과 시간순 보고서 시각 확인, 콘솔 오류 0.
- 빌드 통과. 기존 엔진 eval/번들 크기 경고는 유지. 모바일은 이번에 별도 실기 확인하지 않음.
- 기본값 질문: 활동 기본8, 센터 HP50%, 병행 교대, 09~18시 균등 배분은 임시 설정. 센터 이동 거리·포획률·경험치 곡선은 원작을 완전히 복제한 것이 아닌 기존 근사 로직이며 이후 밸런스 조정 대상. 체육관/진화/타지역 확장은 이번 변경 범위 아님.

## 작업 15 — 플레이 가능한 매니지먼트 화면 개편 (완료, 2026-09-10)

- 확인: 저장소 SPEC.md와 Downloads/spec2.md, 현재 오프닝·협상·맵·하루 진행 코드.
- 완료: 대표실 대시보드/분할 받은 편지함, 도트 파트너 선택 장면(3종·울음소리·재선택·확정), 격자형 박스/파티 이동, 지방 지도/검색/활동 필터, 활동 달력.
- 완료: 날짜·포획·기술·계약·자금·읽은 소식·난수 위치를 보존하는 전체 저장과 복원. 기존 오프닝 저장은 호환. 주급은 7일마다 지급.
- 완료: 실제 계산 단계에 따라 진행 창을 갱신하고 일일 보고서로 이동. 배틀 계산은 Worker에서 실행해 화면을 막지 않음. 진행 실패 시 기존 상태를 유지하고 재시도 가능.
- 사용자 최종 지시 반영: 대회 참가·자동 개최와 대회/리그 메뉴 제거. 일정은 활동·급여 달력으로 교체. 과거 배틀 기록 호환을 위한 엔진/스키마와 숨겨진 개발자 진입은 보존.
- 기본 판단: 훈련은 이전 요청대로 복원하지 않음. 로딩에는 실제 실행한 탐험/휴식/소속사 활동/정산만 완료로 표시.
- 기본 판단: 1~9세대 지방의 지도 표현을 공통화. 현재 데이터가 있는 관동·성도는 플레이 가능, 나머지는 지도 열람만 가능. 임의 NPC·출현표를 확정 데이터로 만들지 않음.
- 확인: 실제 브라우저에서 새 게임→협상→파이리 Lv5 선택→1번도로 포획→꼬렛 Lv3 박스 입고→파티 합류→재접속 후 2일차/파티 보존. 콘솔 오류 없음.
- 자동 검사: management-flow (저장 전후 같은 난수/결과, 포획/기술/파티, 주급, 시장 갱신, 67개 장소 연결, 대회 미개최) 통과. 과거 배틀 스냅샷/브래킷 52경기 검사 통과. 대회 테스트는 정상 플레이와 분리된 과거 호환 테스트다.
- 빌드 통과. 기존 엔진의 eval 경고와 큰 데이터 번들 경고는 남아 있음. 기술 방향 테스트는 외부 데이터 조회가 0건으로 끝나 이번 검증 근거에서 제외함.
- 남은 범위: 호연~팔데아 7개 지방은 공통 스타일의 도식 실루엣 미리보기만 제공. 상세 도시/도로·NPC·포획 데이터 없음. 체육관·뱃지 획득, 연간 시즌, 새 대회 설계도 미구현.
- 기본값 질문: 달력은 3월 1일 시작, 급여일은 7일 주기로 두었음. 실제 시즌 시작일/급여 요일은 후속 기획에 맞춰 조정.
- 최종 배포 빌드 직접 확인: 파티 2마리로 1번도로 NPC에게 승리, 상금 +48, 파이리 Lv6·꼬렛 Lv5 및 전광석화 습득. 하루 처리 Worker 완료, 콘솔 오류 0. 숨겨진 기존 시뮬 HTML도 빌드 산출물에 포함하도록 수정.

> 이 파일만 보면 뭘 끝냈고 다음에 뭘 해야 하는지 알 수 있게 유지한다.

---

## 최근 작업 14: 훈련을 빼고 **맵 탐험**으로 — 포획·NPC 배틀·박스·기술 세팅·연패

> "훈련 기능이 사실 저게 아니야. 컨디션이 체력처럼 있는 것도 아니고.
>  맵에서 어디를 클릭하고 포켓몬 포획, 트레이너 배틀 → 다음 누르면 결과만."

### 지도

HGSS 원작 관동·성도 통합 지도(불바가든 아카이브, 3000x1316)를 링크로 쓴다.
서버가 Referer를 보면 403을 주므로 `<img referrerpolicy="no-referrer">`로 부른다 —
CSS background로는 안 된다(Referer가 붙는다). 지역 탭으로 반씩 잘라 보여준다.

`data/routes.js`에 **67개 장소**(관동 36 / 성도 31): 권장 레벨, 야생 출현표 248항목,
NPC 트레이너 115명(등급·이름·파티·상금 배수). 원작 FRLG/HGSS 기준 1차 근사치이고
전부 데이터라 값만 고치면 된다. 좌표는 그림 위 %라 점 위치도 표에서 조정한다.

### 탐험 (`engine/explore.js`)

맵에서 도로를 누르고 **포켓몬 포획 / 트레이너 배틀**을 고르면 그날의 일이 된다.
[계속]을 누르면 결과가 받은 메시지함에 **글로만** 온다 (관전 없음):

```
1일차  오성: 1번도로로 나갔다. 야생 구구가 나타났다! (Lv3) 구구를 잡았다! → 소속사 박스로 보냈다.
2일차  오성: 1번도로로 나갔다. 소년 지훈이 승부를 걸어왔다! (꼬렛 Lv3, 구구 Lv3)
       소년 지훈에게 이겼다! 상금 48. 파이리가 레벨 7이 되었다. 체력이 부족해 포켓몬센터에 들렀다.
4일차  … 파이리가 레벨 9가 되었다. 파이리는 연막을 배웠다!
9일차  등산가 상철에게 졌다… (10턴) 체력이 부족해 포켓몬센터에 들렀다.
```

- 배틀은 **실제 엔진(@pkmn/sim)**으로 돌린다. 결과만 안 보여줄 뿐이다.
- 포획은 먼저 한 판 붙어 이긴 뒤 확률(레벨 차이 기준)로 잡는다. 잡으면 박스로.
- 상금 = 등급 배수 × 최고 레벨 (원작 공식). 경험치는 상대 레벨 × 계수.
- 배틀 뒤 파티 체력이 절반 아래면 "포켓몬센터에 들렀다"가 붙는다 (`runBattle`이 이제 `hpAfter`를 돌려준다).
- 권장 레벨보다 한참 낮으면 경고를 붙이고, 지도 점이 붉게 뜬다.

### 연패 → 실력 하락 (멘탈이 드디어 쓰인다, §4.5)

지면 `mentalDebuff`가 쌓이고 `effectiveStats()`에서 전 스탯에 빠진다.
**멘탈이 높을수록 덜 깎이고 이기면 빨리 회복한다.** 예전의 컨디션 배율은 뺐다 —
컨디션은 체력 게이지가 아니다. 스쿼드 표에 −n.n로 붉게 표시된다.

### 박스 · 기술 세팅

- 소속사에 `box[]`. 잡은 포켓몬은 여기로 오고, 박스 화면에서 파티(6마리)로 올리고 내린다.
  마지막 한 마리는 못 뺀다.
- 개체에 `learned[]`(지금까지 배운 기술 전부)를 두고, **레벨업이 기술을 덮어쓰지 않는다** —
  자리가 비면 채우고, 꽉 찼으면 플레이어가 프로필에서 체크박스로 4개를 고른다.
- 구구·캐터피 같은 구세대 종은 9세대 학습표가 비어 있어 기술이 없었다. 그 종이 마지막으로
  나온 세대의 표로 떨어지게 고쳤다.

### 뺀 것

- 훈련 화면과 `train:` 행동. 컨디션 게이트·회복·소모 전부. 성장은 탐험으로만.

### 확인한 것

- 오프닝 → 계약 → 맵 → 1번도로 포획 → [계속] → 받은 메시지함, 콘솔 에러 0
- 5일 진행: 구구·캐터피 포획(박스 2), NPC 3승, 자금 3,000 → 3,164, 파이리 Lv5 → Lv10
- 박스→파티, 기술 체크 해제 후 적용 → moves 3개로 반영
- 테스트 6종 + 빌드 통과

### 남은 것

- 지도 점 좌표가 대충이다 — 몇 개는 옆 도로에 찍힌다. 표에서 조정.
- 체육관 관장 8명(뱃지)이 아직 없다. 지금 구조 그대로 `trainers`에 관장을 넣고 이기면 뱃지가
  붙게 하면 된다.
- 포획 확률이 종별 포획률이 아니라 레벨 차이 근사다.
- 센터 방문이 하루를 안 잡아먹는다. 이동 시간도 없다.
- 대회 출전은 일정 화면의 버튼으로 남겨뒀지만, 탐험과 같은 날 못 하는 규칙은 없다.

---

## 최근 작업 13: 화면을 FM으로 갈아엎고, 포켓몬을 직접 키우게 바꿈

**지적받은 두 가지를 그대로 반영했다.** 탭 6개(오늘/트레이너/대회/시장/리그/소식)를
튜토리얼로 다시 끌고 온 게 오해였고, 완성된 6마리를 찍어주던 것도 잘못이었다.

### (1) 탭 6개 → FM식 셸

```
┌──────────────────────────────────────────┐
│ 소속사 · 대표   날짜 자금 주급 평판  [계속 ▶] │
├────────┬─────────────────────────────────┤
│ 소속사  │                                  │
│  받은…  │                                  │
│  스쿼드 │           본문 하나               │
│  훈련   │                                  │
│ 경기    │                                  │
│  일정   │                                  │
│  리그   │                                  │
│ 영입    │                                  │
│  스카우팅│                                 │
└────────┴─────────────────────────────────┘
```

- **받은 메시지함이 허브다.** 대회 결과·레벨업·NPC 동향이 전부 여기로 들어온다
  (기획서 §9 뉴스 파이프라인을 그대로 씀). 안 읽은 건 좌측에 숫자가 붙는다.
- **스쿼드**는 FM식 표다 — 능력치를 값에 따라 색이 붙는 숫자로 보여주고, 행을 누르면
  트레이너 프로필(능력치 / 계약 / 보유 포켓몬)로 들어간다.
- **시간은 상단 [계속] 하나로만 흐른다.** 예전 [다음 날] 버튼과 같은 함수를 쓴다.
- 관전 화면은 그대로 재사용하고 오버레이로만 띄운다.
- `game-view.js`(탭 6개)는 삭제했다.

### (2) 포켓몬을 직접 키운다

`data/pokemon.js` 신규. 예전 `pokemon-pool.js`는 **종족 + 특성 + 성격 + 도구 + 기술 4개**를
통째로 박아둔 "완성된 개체" 표였고, 팀 빌더가 그걸로 Lv50 6마리를 그냥 찍어냈다.
그래서 육성이라는 층이 아예 없었다.

이제 개체는 이렇게 산다:

| | 예전 | 지금 |
|---|---|---|
| 시작 | Lv50 6마리, 노력치까지 완비 | **Lv5 스타터 1마리** |
| 기술 | 표에 적힌 4개 고정 | **레벨업 기술표대로 배운다** (`@pkmn/dex` 학습표) |
| 성장 | 없음 | 경험치(레벨³ 곡선) → 레벨업 → 새 기술 |
| 노력치 | 처음부터 박힘 | 0에서 시작, 훈련으로 쌓음 (252/510 상한) |
| 개체값 | 없음 | 태어날 때 0~31 |

실측: 꼬부기 Lv5로 시작해 11일간 훈련시키니 **Lv10**이 되고 물기·껍질에숨기·고속스핀을
배웠다. 받은 메시지함에 "채아의 꼬부기가 레벨 10 — … 습득"으로 뜬다.

`species-pool.js`는 이제 **종족과 등급만** 들고 있다. NPC는 "이미 굴러가는 세상"이라
등급별 레벨대(16~50)로 파티를 갖고 시작한다.

배틀 엔진에는 `toShowdownBlock()`이 그때그때 텍스트를 만들어 넘긴다 — 레벨·개체값·
노력치·성격·실제 배운 기술이 전부 반영된다.

### (3) 계약을 대화로

`negotiation.js` 신규. 폼에 숫자 넣고 버튼 누르면 즉시 판정이 뜨는 건 협상이 아니라
계산기였다. FM처럼 **왼쪽에서 조건을 짜고 [제안하기]를 누르면 대화창에 한 턴씩 쌓인다.**

```
      블루와의 협상 자리
블루  간판을 본다. … 조건부터 들어보죠.
나                    주급 8, 3년 계약으로 제안드립니다.
블루  영입 개런티가 부족합니다.
블루  주급도 조금 더 봐주셨으면 합니다.
블루  소속사 평판도 조금 더 봐주셨으면 합니다.
```

조건을 고쳐 다시 제안하면 그 아래로 이어진다. 판정 자체는 `engine/contract.js`가 하고
이 파일은 그걸 말로 옮기기만 한다. 계약할 때 **첫 포켓몬(스타터 3종)도 여기서 고른다.**

### (4) 개발자 모드 목적지 정정

"기존 배틀 시뮬 화면"은 탭 6개가 아니라 **`trainer_sim_v2.html`**(단일 파일 프로토타입)이다.
이름 칸의 암호는 이제 그 파일로 넘긴다 — 탭을 지워도 진입점이 살아 있다.

### 확인한 것

- 타이틀 → 이름 → 소속사 → FM 셸 → 튜토리얼 → 대화 협상 → 본편, 콘솔 에러 0
- 채아 계약(주급 16 · 뱃지 80 · 개런티 150) → 꼬부기 Lv5 수령 → 11일 뒤 Lv10
- 테스트: replay-consistency / bracket-integrity / sprite-coverage / anim-orientation /
  battle-tuning / game-loop 통과 (파티가 비동기가 되면서 전부 async로 고침)

### 남은 것

- **대회 출전·일정 화면이 아직 얕다.** FM의 경기 일정/결과 화면 수준으로 더 가야 한다.
- 체육관 8곳 도전, 코치, 유스 자동 육성은 여전히 스키마만 있다.
- 시장(프로 계약) 화면이 없다 — 지금은 유스 계약만 있다.
- 포켓몬 포획·교체·기술 선택 UI 없음. 지금은 레벨업이 기술을 자동으로 덮어쓴다.

---

## 최근 작업 12: 본격 개발 착수 — 오프닝 시퀀스가 새 메인 진입점

지금까지 만든 관리 화면은 **하나도 안 건드렸다.** 새로 짠 건 "진입 흐름"뿐이다 (§0.2).

```
타이틀 (아무 키)
  ↓ 대표 이름          ← "비통한비버통"이면 개발자 모드로 기존 시뮬 직행
  ↓ 소속사 선택         ← 등급 라벨 없이 모기업 자금 / 이사진 기대만
  ↓ 튜토리얼            ← 기존 화면 위에 말풍선만
  ↓ 첫 유스 계약         ← 원클릭 영입 대신 조건 협상
  ↓ [본편] 로스터 잠금 — 뱃지 8개까지 추가 계약 불가
```

### 소속사 선택 — FM처럼 등급을 숨긴다

`PLAYER_AGENCY_CHOICES` 4개. 화면에 나가는 건 **모기업 자금 / 이사진 기대 막대 5칸뿐**이고,
`tier`(대기업/중소…)는 내부 데이터로만 산다. 헤더에 띄우던 `tierLabel`도 뺐다 —
그 자리에는 대표 이름이 들어간다.

| | 모기업 자금 | 이사진 기대 | 실제 자금 | 평판 |
|---|---|---|---|---|
| 신생 소속사 | 1 | 0 | 3,000 | 20 |
| 무지개백화점 스포츠팀 | 2 | 2 | 6,000 | 32 |
| 도라지 트레이더스 | 3 | 3 | 9,000 | 45 |
| 실프주식회사 육성부 | 5 | 5 | 20,000 | 70 |

### 첫 유스 계약 — 원클릭 영입을 걷어냈다

예전 시장 탭은 버튼 한 번에 영입이 끝났다. **"이적료 낼 돈이 있냐"만 묻는 구조라 결정이 없다.**
이제 조건을 짜고 후보가 자기 선호대로 판정한다 (`engine/contract.js`).

계약 항목 5개: 주급 / 영입 개런티 / 프로 승급 시 급여(%) / 뱃지 보너스 / 계약 기간.
**위약금은 없다** — 유스는 신규 영입이라 남의 소속사에서 빼오는 게 아니다 (§5.3).

후보 선호는 전부 같은 모양이라 후보를 늘릴 땐 값만 채우면 된다:

```
{ want: 기준값, weight: 중요도, kind: 'more' | 'near' }
```

`near`는 계약 기간처럼 **길다고 좋은 게 아닌** 항목용이다.

| 후보 | 성향 | 실측 (신생 소속사, 평판 20) |
|---|---|---|
| 오성 | 무관심 — 주급 7만 넘으면 된다 | 기본 조건 그대로 수락 |
| 채아 | 성과중시 — 뱃지 보너스에 반응 | 주급 16 + 뱃지 80 + 개런티 150이면 수락 |
| 블루 | 평판중시 — 간판부터 본다 | 평판 20이면 주급 22·개런티 600·승급 150%·5년까지 필요 |

평판은 **제안으로 못 바꾸는 항목**이라, 간판이 약하면 나머지로 메워야 한다.
거절하면 부족한 항목을 짚어준다 ("영입 개런티가 부족합니다").

### 세이브 — 씨앗만 저장한다

전체 상태를 직렬화하지 않는다. **시드 + 이름 + 소속사 + 계약 내용만** 남기고 리그는 시드로
다시 만든다. 스키마가 계속 바뀌는 단계에서 스냅샷을 저장하면 갱신마다 세이브가 깨진다.
타이틀에서 "이어하기"로 돌아온다 (실측: 이대표 · 무지개백화점 · 오성 계약 그대로 복원).

### 확인한 것

- 타이틀 → 이름 → 소속사 → 튜토리얼 6단계 → 계약 → 본편, 콘솔 에러 0
- 개발자 모드: 이름에 암호 → 기존 시뮬(트레이너 4명, 시장 열림)로 직행
- 시장 탭이 "지금은 계약 불가 · 뱃지 0/8"로 잠김
- 계약 시 개런티가 자금에서 빠짐 (3,000 → 2,400)
- 이어하기 복원

### 남은 것 (다음 차례)

- **체육관 8곳 도전 자체가 아직 없다.** 뱃지 카운터와 잠금만 있고 도전 UI는 §3.10대로 따로.
- 튜토리얼 문구는 자리만 잡아둔 수준 — 실제 플레이 감각에 맞춰 다듬어야 한다.
- 유스 계약 조항(프로 승급 인상·뱃지 보너스)은 저장만 되고 **아직 지급 로직이 없다** (§0.2 스키마 우선).
- SPEC.md에 §3.10 진입 흐름과 §5.3.1 유스 계약을 이번에 받아 적었다.

---

## 최근 작업 11: 상대가 쓸 때 연출이 뒤집히던 버그 — `focus`를 무시하고 있었다

> "대지의힘은 아래에서 위인데 반대고, 스톤샤워는 위에서 아래인데 반대야.
>  치근거리기는 이펙트가 본인 자리에 남고 시전자만 상대쪽에 가 있어."

### 원인 — 좌표계를 통째로 180° 돌리고 있었다

지난번에 방향 뒤집기를 이렇게 처리했다: 두 앵커를 실제 스프라이트 위치에 맞추면
상대가 시전자일 때 배율 부호가 **가로 세로 둘 다** 음수가 되어 자연히 180° 돌아간다고.
화염방사로 확인했을 땐 맞았는데, **그게 화염방사라서 맞았던 것**이다.

원본 데이터에는 조각마다 `focus`가 붙어 있고, 그게 기준점을 정한다:

| focus | 뜻 | 예 | 좌우 | 위아래 |
|---|---|---|---|---|
| 1 TARGET | 대상 기준 | 스톤샤워 낙석, 대지의힘 분출, 치근거리기 | 뒤집음 | **고정** |
| 2 USER | 시전자 기준 | 칼춤 | 뒤집음 | **고정** |
| 3 USER_TARGET | 두 점을 잇는 축 | 화염방사, 섀도볼 | 뒤집음 | 뒤집음 |
| 4 SCREEN | 필드 기준 | 지진 | 뒤집음 | **고정** |

**축을 따라가는 3번만 통째로 뒤집는 게 맞다.** 나머지는 좌우만 뒤집고 위아래는
세상 기준이어야 한다 — 중력은 누가 기술을 쓰든 아래로 당긴다.
전부 180° 돌리니 돌이 솟고 분출이 꽂혔다.

실측한 좌표 범위가 그대로 증거다:

```
스톤샤워    focus 1  x[84..184]  y[-120..-19]   대상(128,-64) 위쪽 = 하늘
대지의힘    focus 1  x[107..148] y[-35.5..-30.5] 대상 발밑
칼춤        focus 2  x[-34..32]  y[-53.5..-2.5]  시전자 머리 위
화염방사    focus 3  x[10.5..117] y[-60..2.5]    시전자에서 대상까지
```

### 치근거리기는 원인이 하나 더 있었다

`focus 1`이라 대상 기준인데, 나는 **매 프레임 시전자 앵커를 원점으로 다시 잡고 있었다.**
접촉기의 시전자 앵커는 앞으로 파고들며 x가 0→124로 움직인다. 원점을 거기 맞추니
그림이 시전자를 따라 **뒤로 끌려가서**, 몸만 상대에게 가고 이펙트는 제자리에 남았다.

→ 원점은 첫 프레임(쉬는 자세)으로 고정하고, 앵커의 움직임은 스프라이트를 옮기는 데만 쓴다.

### 같은 자리에서 날 만한 것 차단

좌표 변환을 `makePlacement()` 하나로 모아 focus별 기준점·배율을 한곳에서 정하게 했다.
그리고 `test/anim-orientation.mjs`로 지킨다 — 풀에 실제로 쓰이는 기술 **104종**을
p1이 쓸 때와 p2가 쓸 때로 각각 계산해서:

- focus 1·2·4 조각의 **기준점 대비 세로 오프셋이 양쪽에서 같은가** (중력이 안 뒤집혔나)
- 대상 기준 연출의 **무게중심이 대상 쪽에 모여 있나** (치근거리기가 시전자에게 붙던 것)

기 모으는 앞부분은 시전자 근처에 그려지는 게 정상이라(용성군·기가톤해머·브레이브버드)
조각 하나씩이 아니라 연출 전체의 무게중심으로 본다.

화면 대조도 했다: 스톤샤워는 대상 머리 위에서 쏟아지고, 대지의힘은 대상 발밑에서 솟고,
치근거리기는 양방향 모두 대상에게 붙는다. 화염방사·지진은 그대로다.

---

## 최근 작업 10: 최신 세대도 움직이는 BW 도트로 — 커뮤니티 시트 도입

> "더시마사리처럼 다른 애들도 커뮤니티 긁어오면 안돼? 정지 도트된 애들만 교체할 때
> 깜박거리는 버그가 있어."

둘 다 됐다. 그리고 **찾아보니 이미 우리가 쓰는 저장소에 있었다.**

### 찾은 것 — 포켓로그의 포켓몬 스프라이트 시트

기술 이펙트·효과음으로 이미 쓰고 있던 `pokerogue-assets`에 포켓몬 스프라이트도 들어 있다.
아틀라스(PNG 시트 + JSON) 형태고, 두 갈래다:

- `images/pokemon/{번호}` — 5세대 원본 BW 애니메이션
- `images/pokemon/exp/{번호}` — **커뮤니티가 BW 그림체로 그린 6세대 이후** 애니메이션

실측: 풀 53종 x 앞뒤 106장 중 **98장(92%)이 애니메이션.** 정지컷은 4종뿐인데
(드닐레이브·저승갓숭·파라블레이즈·킬라플로르) 이 넷은 PokeAPI에도 쇼다운에도
움직이는 그림이 아예 없다 — 지금 세상에 존재하지 않는 것이다.

### 프레임 간격은 실측으로 잡았다

아틀라스에 재생 속도가 안 적혀 있는 게 많아서, 원본 GIF의 프레임 지연을 파싱해 대조했다:

| 종 | 원본 GIF | 포켓로그 시트 | 항목당 |
|---|---|---|---|
| 한카리아스 | 90프레임 7.2초 | 160항목 | 45ms |
| 리자몽 | 72프레임 7.3초 | 144항목 | 51ms |
| 해피너스 | 47프레임 4.7초 | 96항목 | 49ms |

기본 폴더 시트는 원본 프레임을 **2개씩 늘려놨다.** → 50ms(20fps)면 원본과 길이가 맞는다.
`exp` 폴더는 1:1이라(더시마사리 GIF 87프레임 ↔ exp 87항목) 100ms.
아틀라스가 `duration`을 직접 적어놨으면 그걸 쓴다.

곁다리: 아틀라스가 두 형식으로 온다 (TexturePacker / Aseprite). 둘 다 받는다.
그리고 `sourceSize`가 내용보다 큰 시트가 있어서(두드리짱 150x136) 그대로 쓰면 덩치가
과장된다 — 프레임들의 **실제 그려지는 범위**를 합쳐서 잘라 쓴다.

### 깜박임 버그 — 원인은 "빈 칸을 피하려던 것"이었다

교체할 때 새 그림이 다 내려올 때까지 **옛 그림을 남겨두고 있었다.** 빈 칸을 안 보이게
하려던 건데, 정지컷 종은 후보를 여러 개 훑느라 로드가 길어져서 옛 도트가 눈에 띄게 남았다.
그게 "에이스번이 웨이니발로 바뀔 때 섞여 깜박이는" 현상이었다.

→ 교체 순간 **즉시 비운다.** 대신 재생 시작할 때 프로토콜의 `|poke|` 줄을 읽어
**양 팀 12마리를 미리 받아둔다.** 실측으로 빈 칸이 보이는 시간은 전체의 7%였고,
그나마도 첫 등장 때뿐이다 (틀린 포켓몬이 보이는 것보단 낫다).

### 덤

- 얼음 상태의 "도트 멈춤"이 훨씬 깔끔해졌다. 예전엔 GIF를 캔버스에 떠서 바꿔 끼웠는데,
  이제 시트 재생기를 그냥 세우면 된다.
- 정지 PNG용 여백 보정(`PADDED_SHIFT`)은 폴백 경로에만 남았다 — 지금 풀에선 안 쓰인다.

---

## 최근 작업 9: 스프라이트를 전부 BW 도트로 통일

> "더시마사리만 정상이고 나머지 최신 포켓몬들은 다 비정상이야. 해상도만 줄인 것들이야."

정확한 지적이었다. **해상도 문제가 아니라 그림체가 아예 다른 세트였다.**

### 원인

폴백 경로 이름이 `other/showdown/`이라 "쇼다운의 BW 도트"인 줄 알았는데,
실제로는 **최신 고해상도 스프라이트 세트**다. 크기부터 다르다:

| 종 | BW 폴더 | `other/showdown/` |
|---|---|---|
| 아머까오 | 없음 | **200x162** |
| 두드리짱 | 없음 | **178x95** |
| 떨구새 | 없음 | **166x112** |
| 더시마사리 | **90x77** | 113x82 |

BW 도트는 40~110px인데 폴백은 150~200px이다. 같은 칸에 욱여넣으니
축소되면서 도트가 뭉개졌다 — 그래서 "해상도만 줄인 것"처럼 보였다.

**더시마사리(#748)만 멀쩡했던 이유도 확인했다.** 7세대인데도 BW 애니메이션 폴더에
들어 있다 (`90x77`). 커뮤니티가 BW 그림체로 그려 넣은 것이라 진짜 BW 도트다.

그리고 **무쇠무인·다투곰(붉은 달)은 아예 실루엣**으로 떨어지고 있었다 — 두 폴더 어디에도 없었다.

### 고친 방식 — 후보를 BW 계열로만 줄 세운다

```
1. PokeAPI  BW 애니메이션 GIF   진짜 DS 원본, 움직인다
2. 쇼다운   gen5ani GIF         같은 그림체, PokeAPI 미러보다 최신 (무쇠무인·위대한엄니…)
3. PokeAPI  BW 정지 PNG         BW 그림체 정지컷
4. 쇼다운   gen5 정지 PNG       뒷모습 커버리지가 여기만 완전하다
5. 폼 전용 그림이 없으면 기본 종으로 (다투곰(붉은 달) → 다투곰)
```

실측 결과 **106장(53종 x 앞뒤) 전부** 잡힌다 — 애니메이션 74 / 정지 32, 실패 0.
`test/sprite-coverage.mjs`로 지킨다.

### 곁다리로 나온 두 가지

**정지 PNG는 96x96 고정 캔버스라 여백이 붙어 있다.** 잘린 GIF와 같은 잣대로 크기를 맞추려면
여백을 잘라내야 해서, 로드 후 캔버스로 내용 범위를 재서 잘라낸다. (예전에 손으로 관리하던
`sprite-fit.js` 보정표를 런타임 계산으로 대체한 셈이다)

**쇼다운 서버는 `Access-Control-Allow-Origin`을 안 보낸다.** 그런데 `crossOrigin`을 붙이면
이미지 로드가 **아예 실패한다** — 실제로 위대한엄니 뒷모습이 실루엣으로 떨어져서 발견했다.
그래서 쇼다운 쪽은 crossOrigin 없이 부르고, 캔버스가 오염돼 못 자르는 16장은
위치 보정값으로 발밑을 맞춘다.

---

## 최근 작업 8: 폴백 기술 해소 · 최신 세대 파티 · 상태이상 연출 · 트레이너 표시 · 일시정지

### (1) CSS 폴백으로 남아 있던 5종 — 원인이 두 가지였다

**바디프레스 · 그래스슬라이더 · 뛰어오르다는 사실 데이터가 있었다.** 우리가 못 읽었을 뿐이다.

- 최상위가 **배열**인 형태가 따로 있었다 (`[{...}]`). flat / keyed만 받고 있어서 걸렀다.
- `graphic: ""`인 정의를 "형식 오류"로 버리고 있었다. 실은 시트 없이
  **포켓몬 스프라이트 자체만 움직이는** 연출이다 — 바디프레스는 몸으로 짓누르는 게 전부다.

그래서 지난번에 "남은 것"으로 미뤄뒀던 **앵커 구동**을 이번에 넣었다.
`target 0/1` 앵커에는 스프라이트의 이동·확대·투명도·색조가 들어 있다. 이걸 실제
`#sp-pX`에 얹으니 세 기술이 그대로 살아났고, 지진의 시전자가 뛰어오르는 것 같은
다른 기술의 몸동작도 같이 살아났다. (원본이 스프라이트를 몰 때는 CSS 돌진 모션을 얹지 않는다)

**테라버스트·블러드문은 데이터가 진짜로 없다.** `ANIM_ALIAS`에 비슷한 기술을 적어뒀다
(테크노버스터 / 하이퍼빔). 8·9세대 65종 기준 **65/65**가 됐다.

### (2) 최신 세대 파티

내 소속사(신생 소속사)가 8·9세대만 뽑도록 `modern` 프로필을 넣었다
(`latest: true` + `latestOnly`). 풀에 17종을 추가 — 위대한엄니 · 무쇠무인 · 도치갑 ·
드닐레이브 · 저승갓숭 · 다투곰(붉은 달) · 에이스번 · 마스카나 · 웨이니발 · 라우드본 ·
파라블레이즈 · 킬라플로르 · 두드리짱 · 엑스레그 · 빠르모트 · 절벼게 · 떨구새.

시작하자마자 화염볼·기가톤해머·격노의주먹 같은 기술을 실제 경기에서 보게 된다.
(NPC 쪽은 그대로라 신구 대비도 같이 보인다)

### (3) 상태이상 — 연출도 소리도 실제 게임 것으로

공통 애니메이션이 저장소에 있었다. 전용 음원까지 딸려 있다:

| 상태 | 애니메이션 | 음원 |
|---|---|---|
| 화상 | `common-burn` | `PRSFX- Burn.wav` |
| 독·맹독 | `common-poison` | `PRSFX- Poison.wav` |
| 마비 | `common-paralysis` | `PRSFX- Paralysis.wav` |
| 잠듦 | `common-sleep` | `PRSFX- Sleep.wav` |
| 얼음 | `common-frozen` | `PRSFX- Frozen.wav` |

걸릴 때 한 번, 그리고 **지속 피해가 들어올 때마다 매 턴 다시 돈다** (실기와 같다).

거기에 걸려 있는 동안 계속 보이는 표시도 붙였다 — 화상은 불꽃과 주황 맥동,
독은 해골과 보라 물듦, 마비는 노란 스파크, 잠듦은 어두워짐.
**얼음은 도트가 멈춘다** — GIF는 재생을 세울 수 없어서 그 순간 프레임을 캔버스에 떠서
바꿔 끼운다 (그래서 스프라이트에 `crossOrigin`이 필요하다).

### (4) 남은 포켓몬 수 · 트레이너 이름표

- HP 박스 아래에 몬스터볼 줄을 넣었다. `|teamsize|`로 총원을, `|faint|`로 남은 수를 센다.
- **HP 박스에서 "상대 " 접두사를 뺐다.** 대신 빈 모서리(오른쪽 위 / 왼쪽 아래)에
  트레이너 이름과 소속을 띄운다. 누가 누구 포켓몬인지 훨씬 빨리 잡힌다.
  자막은 실기 문법대로 "상대 마기라스의 스톤에지!"를 유지한다.
- 곁다리로 잡은 버그: 쓰러진 줄(`0 fnt`)에는 최대 HP가 안 실려서 박스에 **"0/null"**이 떴다.
  이전 값을 이어 쓰도록 고쳤다.

### (5) 일시정지 버튼

관전 창에 `⏸ 일시정지`를 넣었다. 재생 루프가 줄 경계마다 게이트를 지나가고,
풀면 기다리던 지점이 한꺼번에 깨어난다. 실측: 멈춘 3초 동안 로그 0줄, 풀고 3초에 4줄.

### (6) 스톤샤워에서 바람 소리가 나던 버그

원인은 **같은 음원이 겹쳐 쌓인 것**이었다. 스톤샤워는 `PRSFX- Rock Slide.wav`를
프레임 0·9·16에서 음정 90/100/110으로 세 번 부르는데, 이 파일이 **1.83초**다.
간격이 0.4초라 세 겹이 거의 통째로 겹쳐 웅웅거리는 잡음이 됐다.

→ 같은 소리가 이미 울리고 있으면 **끊고 다시 튼다.** 의도대로 "돌 세 개가 차례로
떨어지는" 소리가 된다. (지진의 연속 굉음도 같이 정리됐다)

### (7) 기절 소리

합성음은 이미 다 걷어냈으니 들리던 건 공식 `se/faint.wav`였는데, 실기와 달랐다.
**실기는 쓰러질 때 그 포켓몬이 운다.** 울음소리를 먼저 내고 미끄러지는 소리를 뒤에 깔았다.
(그래서 `faint` 연출 지시에 종족을 실어 보낸다)

### (8) 이름표 생성 범위를 넓혔다

지난번 기술 표에 이어 **종족·특성까지** 생성으로 옮겼다 (`tools/gen-ko.mjs` →
`src/data/ko.gen.js`, 기술 916 / 종족 1079 / 특성 316). 그래서 이번에 17종을 넣으면서
한글 이름을 한 줄도 손으로 안 적었다 — 폼 변형 3개만 OVERRIDE에 뒀다.

---

## 최근 작업 7: 소리 정리 + HP 바 감소 연출 + 최신 세대 기술 검증

### (1) 임시 합성음 전부 제거 — 공식 음원만 남김

> "이전의 소리와 공식 소리가 같이 들어가서 이상한 느낌이야."

맞다. 지난번엔 공식 음원이 없는 이름만 합성음으로 메웠는데, 그 둘이 한 화면에서
번갈아 나니 이질적이었다. **합성 코드(오실레이터·노이즈·SOUNDS 표)를 통째로 걷어냈다.**
이제 `SAMPLE_URLS`에 있는 파일만 울리고, 없는 이름은 조용히 넘어간다.

실기에 없는 소리도 같이 정리했다:

| 뺀 것 | 이유 |
|---|---|
| 효과굉장 / 반감 / 무효 전용음 | 실기는 **타격음의 세기**로 구분한다 (hit_weak / hit / hit_strong) |
| 급소 전용음 | 실기에 없다. 강타격음을 쓴다 (쓰던 `crit_throw`는 몬스터볼 크리티컬 캐치음이라 아예 딴 소리였다) |
| 빗나감 · 날씨 · 특성 | 실기는 자막만 뜨고 효과음이 없다 |
| 원형별 타격음 (베기·물기·주먹…) | 기술 애니메이션의 PRSFX가 이미 그 역할을 한다 |

남은 소리는 전부 PokeRogue `audio/se`의 실제 파일이다 —
`hit_weak` / `hit` / `hit_strong` / `stat_up` / `stat_down` / `restore` / `faint` /
`low_hp` / `pb_rel` / `level_up_fanfare`. 승리 팡파르는 이번에 붙였고,
HP가 빨간 구간에 들어가면 `low_hp` 경고음이 한 번 울린다.

### (2) HP 바가 사라지듯 지워지던 문제

원인은 단순했다. `drawHpBox()`가 매번 `innerHTML`로 박스를 통째로 다시 그려서
**바 요소 자체가 교체됐다.** CSS에 `transition: width .5s`가 걸려 있었지만 새 요소라
전환이 걸릴 대상이 없었다 — 그래서 지워졌다 다시 나타나는 것처럼 보였다.

- 뼈대는 **개체가 바뀔 때만** 만든다 (이름·레벨·성별이 키)
- 그 뒤로는 폭·색·실수치만 프레임 단위로 굴린다 → 실기처럼 깎여 내려간다
- 감소 속도는 실기에 맞춰 **양에 비례** (가득 찬 바 = 1.5초, 배속 반영)
- 실수치(110/183)도 바와 같이 세어 내려간다

**`requestAnimationFrame`이 아니라 타이머로 굴린다.** rAF는 탭이 가려지면 아예 안 도는데,
재생이 그 프레임을 기다리므로 배틀이 통째로 멈춰버린다 (실제로 재현했다).
타이머는 백그라운드에서 느려질 뿐 멈추지는 않는다.

### (3) 이펙트 시점과 HP 계산 시점이 어긋나던 문제

`playBattleLog`가 **자막을 찍기도 전에** `drawField()`로 화면을 갱신했다.
그래서 "○○의 섀도볼!" 자막이 뜨기 전에 이미 바가 줄어 있었다.

→ 데미지가 들어가는 줄은 화면 갱신을 **피격 순간까지 늦춘다.**
`playAnim(anim, token, onImpact)`이 타격음과 같은 시점에 `onImpact()`을 부르고,
바가 다 내려간 뒤에 다음 줄로 넘어간다. 연출이 없거나 중단된 경우를 위한 안전망도 뒀다.

실측 (섀도볼 1발, 2배속):

```
6496ms  자막 "해피너스의 섀도볼!" 완성
        ↓ (섀도볼 애니메이션 재생)
8509ms  p2 100% → 98.08%   ← 맞는 순간부터
8763ms  p2 82%             ← 254ms에 걸쳐 감소
```

### (4) 최신 세대 기술 검증 — `test/latest-gen-moves.mjs`

8세대 30종 + 9세대 35종을 훑었다.

| 항목 | 결과 |
|---|---|
| 엔진이 아는 기술 | **65/65** |
| 애니메이션 데이터 있음 | **60/65** (없는 5종은 CSS 폴백 — 화면 안 깨짐) |
| 한글 이름표 | 처음엔 **4/65** ← 자막에 영문이 떴다 |

애니메이션 없는 5종: 바디프레스 · 그래스슬라이더 · 테라버스트 · 뛰어오르다 · 블러드문.
파이어볼(`GEN8- PyroBall`) · 덩굴방망이 · 아머캐논 등은 전용 시트로 정상 재생을 화면 대조로 확인했다.

**한글 이름표가 진짜 구멍이었다.** 손으로 적던 73개 표에서 7개가 틀리기까지 했다:

| 기술 | 우리 표 | 실제 |
|---|---|---|
| Zen Headbutt | 제트헤드 | **사념의박치기** |
| Sucker Punch | 놓치지않기 | **기습** |
| Knock Off | 손버릇 | **탁쳐서떨구기** |
| Coil | 사리기 | **똬리틀기** |
| Pain Split | 고통나누기 | **아픔나누기** |
| Crabhammer | 크랩해머 | **집게해머** |
| Recover | 자기재생 | **HP회복** |

→ `tools/gen-move-ko.mjs` 신규. PokeRogue 로케일(ko/move.json)에서 **916종**을 생성해
`src/data/move-ko.gen.js`로 떨군다. 손으로 고칠 건 `ko.js`의 `MOVE_KO_OVERRIDE`에 적으면
생성표를 덮어쓴다. 참고로 파이어볼의 공식 한글명은 "화염볼"이라 추측했으면 틀렸을 것이다 —
그래서 짐작하지 않고 로케일에서 가져왔다.

`validate-pool.mjs`에 이름표 누락 검사를 추가해서, 앞으로 풀에 종을 늘렸을 때 조용히 비지 않게 했다.

### 남은 것

- 연출 원형 표(`MOVE_ANIM`)는 8·9세대 기술 65종 중 4종만 등록돼 있다. 나머지는 타입
  기본값으로 떨어지는데, 애니메이션 데이터가 있는 기술은 어차피 그쪽이 쓰이므로 문제는 없다.
  CSS 폴백으로 떨어지는 5종만 나중에 손보면 된다.

---

## 최근 작업 6: 합성음·CSS 연출 → **실제 포켓몬 에셋**으로 교체

> "임시용 소리지? 왜 포켓몬 에셋을 안쓰는거야? 포켓몬 소리가 아니잖아. 포켓몬 이펙트도 아니고."

맞는 지적이었다. 스프라이트는 이미 실제 도트를 링크로 가져다 쓰면서 소리·이펙트만
직접 만든 건 앞뒤가 안 맞았다. 원래 내 반대 논지는 **"저장소에 복제해서 재배포하지 말자"**였는데,
링크 참조는 스프라이트와 똑같은 태도다. 그래서 같은 방식으로 전부 갈아끼웠다.

### 에셋 출처를 한 파일에 가둠

`src/data/battle-assets.js` 신규. 모든 외부 URL이 여기에만 있다.
나중에 오리지널 IP나 CC0 소재로 바꿀 때 **이 파일만 고치면 된다** (`spriteUrl()`과 같은 설계).

| 무엇 | 어디서 |
|---|---|
| 울음소리 | `PokeAPI/cries` — `legacy/<도감번호>.ogg`, 없으면 `latest/` |
| 공용 효과음 | `pokerogue-assets` `audio/se/*.wav` |
| 기술 효과음 | `pokerogue-assets` `audio/battle_anims/PRSFX- *.wav` |
| 기술 프레임 데이터 | `pokerogue-assets` `battle-anims/<kebab>.json` |
| 기술 스프라이트시트 | `pokerogue-assets` `images/battle_anims/<graphic>.png` |

**실측 확인: 16개 음원 URL 전부 200.** (SE 13 + 울음소리 2 + PRSFX 1)

### 소리 — 샘플 우선, 합성은 폴백으로 남김

`sfx.js`의 호출부는 그대로 두고 재생 경로만 바꿨다.
`playUrl()`이 버퍼를 캐시하고, 실패한 URL은 `failed` 집합에 넣어 다시 시도하지 않는다.
받아오지 못하면 기존 합성음으로 떨어지므로 **오프라인에서도 소리가 난다.**

- `playCry(도감번호)` — 출전할 때 울음소리. legacy → latest 폴백
- `playAnimSound(resourceName, {volume, pitch})` — 기술 프레임에 걸린 PRSFX 음원

### 기술 이펙트 — RMXP 프레임 데이터를 캔버스로 직접 재생

`src/ui/move-fx.js` 신규. 씬 위에 `.fxcanvas`를 덮고 프레임을 잘라 그린다.
데이터 구조를 실측으로 파악해서 맞췄다:

**(1) 조각의 `target`은 "그릴 대상"이 아니라 좌표 앵커였다**

| 값 | 뜻 | 데이터에서의 위치 |
|---|---|---|
| 0 | 시전자 앵커 | 항상 (0, 0) |
| 1 | 대상 앵커 | 항상 (128, −64) |
| 2 | **실제 그림** | 위 두 점이 잡아주는 좌표계 안 |

0/1은 포켓몬 스프라이트 자리를 표시하는 마커라 **시트에서 그리면 안 된다.**
처음엔 전부 그려서 엉뚱한 조각이 끼어 있었다.

**(2) 방향 뒤집기 — 배율 부호로 해결**

원본은 "시전자가 좌하단, 대상이 우상단"으로 그려져 있다. 두 앵커를 실제 스프라이트
위치(`#sp-p1` / `#sp-p2`)에 맞추는 배율을 구하면, 상대가 시전자일 때 부호가 자연히 음수가
되어 통째로 180° 돌아간다. 별도 미러 코드가 필요 없다.

**(3) 자기 대상 기술이 한 점으로 뭉개지던 버그**

칼춤처럼 시전자=대상이면 두 앵커가 겹쳐 배율이 0이 된다 → 모든 조각이 한 점에 모였다.
(우연히 칼춤은 그럴듯해 보여서 처음엔 못 잡았다.)
→ 방향·배율만 **반대편 스프라이트에서 빌려온다.**
   덧붙여 `focus === 1`(대상 기준) 조각은 대상 앵커만큼 당겨줘야 한다 — 용의춤의 그림이
   x≈128 근처에 그려져 있어서, 안 당기면 상대 진영에 떠버린다.

**실측 확인** (프레임 고정 후 화면 대조):

| 기술 | 결과 |
|---|---|
| 화염방사 p1→p2 / p2→p1 | 시전자 입에서 대상까지 불줄기. 양방향 대칭 ✓ |
| 스톤샤워 | 대상 위로 돌 낙하 ✓ |
| 폭포오르기 | 대상 자리에서 물 폭발 ✓ |
| 지진 | 필드 전체에 파편 ✓ |
| 칼춤 | 시전자 주위에 검 3자루 ✓ |
| 용의춤 (상대) | 상대 주위에 소용돌이 ✓ |
| 자기재생 | 시전자 위로 반짝임 상승 ✓ |
| 섀도볼 | 시전자 → 대상으로 구체 이동 ✓ |

풀에 쓰는 기술 30종을 전수 조회한 결과 **전부 실제 데이터가 존재한다.**
없는 기술은 `loadAnim()`이 `null`을 돌려주고 기존 CSS 원형 연출로 떨어진다 — 화면이 안 깨진다.

### 라이선스 — 지난번 판단을 사용자가 뒤집었다

지난 세션에서 "라이선스가 걸려서 보류"로 결론냈는데, 사용자가
**"어차피 전체 공개 코드로 할거라 일단 그냥 이펙트랑 그런거 다 진행해"**라고 결정했다.
그 결정 자체는 사용자 몫이고, 대신 **사실관계를 정확히 적어뒀다** (`CREDITS.md`).

지난번 조사에서 한 가지 정정: PRSFX 음원을 "PokeRogue 자체 제작(AGPL)"이라고 적었는데
`audio/REUSE.toml`을 다시 확인하니 **`LicenseRef-POKEMON-REBORN`**이다. `audio/se/*.wav`는
`LicenseRef-FAIR-USE`(게임 원본). 루트 `REUSE.toml`의 AGPL 표기는 폴더별 `REUSE.toml`이 덮어쓴다.

### 남은 것

- 앵커(0/1) 조각에는 **포켓몬 스프라이트 자체의 움직임**이 들어있다
  (지진의 시전자 앵커 y가 −36까지 올라간다 = 점프). 지금은 안 쓰고 기존 CSS 모션을 유지 중.
  적용하면 더 실기에 가까워지지만 CSS 클래스 트랜스폼과 충돌하니 별도 작업으로 둔다.
- `hue` / `tone` / `blendType` 미적용 (원본 데이터에 있으나 우리 화면에선 티가 안 난다)

---

## 최근 작업 5: HP 바 버그 3종 + 무도구 + 기술별 모션 + 효과음

### HP 바가 늘었다 줄었다 하던 버그 — 원인 3개였다

로그 수치는 맞는데 화면 바만 튀었다. 실측으로 파보니 원인이 셋이었고 전부 잡았다.
(진단 스크립트로 9경기를 훑어 "설명 안 되는 HP 상승"을 세는 방식으로 확인: **78건 → 0건**)

**1) 부연 설명 줄이 바를 되돌리고 있었다 (78건 중 대부분)**

쇼다운 프로토콜은 `-supereffective`류를 **데미지 줄보다 먼저** 보낸다.
그래서 순서가 이렇게 됐다:

```
|move|            → 기술 줄 (나중에 데미지 반영된 상태로 갱신됨)
|-supereffective| → "효과가 굉장했다" 줄이 **데미지 전** 상태를 들고 있음
|-damage|         → 기술 줄만 갱신
```

재생하면 기술 줄이 바를 22%로 내린 직후, "효과가 굉장했다" 줄이 100%로 되돌렸다.
→ 이 부연 줄들(`효과가 굉장/별로/없다`, `급소`, `빗나감`, `실패`)에 `noField`를 달아
   화면 상태를 아예 안 건드리게 했다. 순수 설명이라 그게 맞다.

**2) 자기 회복기가 "자기를 때린 것"으로 처리됐다**

자기재생·날개쉬기처럼 자신을 대상으로 하는 회복기는 대상 슬롯이 시전자와 같아서,
공격 데미지 병합 분기에 그대로 걸렸다. 회복인데 피격 플래시가 뜨고 HP도 데미지처럼 표기됐다.
→ 병합은 `-damage`일 때만 하도록 제한.

**3) 고통나누기(Pain Split)가 화면에 안 잡혔다**

`-sethp`로 HP를 직접 덮어쓰는데 줄을 안 만들어서, 변화가 다음 줄에 가서야 튀어나왔다
(아보크 76% → 84%가 "턴 18" 줄에서 갑자기 나타남). → `-sethp`도 줄을 만들도록 수정.

### 도구 전부 제거

풀의 거의 모든 종이 남은음식을 들고 있어서 매 턴 회복이 끼어들었다.
`USE_ITEMS = false`를 기본으로 두고 팀 조립에서 도구를 빼도록 했다.
고정 프리셋(teams.js)에서도 제거. **각 항목의 `item` 필드는 남겨뒀다** — §0.2 스키마 유지,
나중에 켜고 싶으면 플래그 하나만 바꾸면 된다.

### 기술별 고유 모션

`src/data/move-anim.js` 신규 — 기술 → **연출 원형(archetype)** 표. 15종:

| 원형 | 연출 | 예 |
|---|---|---|
| contact / punch | 몸으로 부딪힘 / 한 점 강타 | 누르기, 인파이트 |
| slash / bite | 베는 자국 / 물어뜯기 자국 | 섀도클로, 깨물어부수기 |
| beam | 시전 후 광선이 뻗음 | 화염방사, 10만볼트 |
| ball | 구체가 날아감 | 섀도볼, 기합구슬 |
| multi | 빠른 연타 3회 | 드래곤애로, 스케일샷 |
| quake | **화면 흔들림 + 지면 충격파** | 지진 |
| rocks / meteor | 위에서 낙하 | 스톤샤워 / 용성군 |
| wind | 바람 베기 호 | 에어슬래시 |
| selfBuff / heal | 자기 발광 + 상승 입자 | 칼춤 / 자기재생 |
| status | 고리가 퍼져 감쌈 | 맹독, 도깨비불 |
| drain | 때리고 기운을 빨아옴 | 기가드레인 |

표에 없는 기술은 타입 기본값으로 떨어져서 **미등록이어도 화면이 안 깨진다.**
효과 판정(굉장/별로/급소)도 기술 줄에 실어 보내 타격 강도가 달라진다.

### 효과음 — Web Audio로 직접 합성

> **최근 작업 6에서 실제 포켓몬 음원으로 교체됐다.** 아래 합성음은 폴백으로만 남아 있다.

`src/ui/sfx.js` 신규. **외부 음원 파일이 0개다** — 오실레이터/노이즈로 그 자리에서 만든다.

- 라이선스 문제가 원천적으로 없다 (지난번 포켓로그 검토에서 걸렸던 지점)
- 도트 화면에 레트로 합성음이 오히려 잘 맞는다
- 로딩·용량이 0이라 첫 재생이 즉시 난다

22종: 타격 3단계(약/보통/강), 베기·물기·주먹, 광선·구체·바람·낙석·지진·운석,
효과굉장·반감·무효·급소·빗나감, 랭크상승·하락·회복·상태이상, 출전·기절·승리·날씨·특성.

나중에 실제 음원(CC0 팩 등)으로 바꾸려면 `SAMPLE_URLS`에 경로만 넣으면 된다 —
그 이름이 있으면 합성 대신 파일을 재생한다. **호출부는 그대로.**

브라우저 자동재생 정책 때문에 첫 클릭에서 오디오를 깨우도록 걸어놨고, 관전 창에 켜기/끄기 체크박스가 있다.

---
## 최근 작업 4: 배틀 화면을 5세대 실기에 맞춰 전면 재작업

BW2 실기 스크린샷과 대조해서 고쳤다. 지적받은 항목 전부 처리.

### 자막 박스 (가장 큰 변화)

**이전:** 로그 한 줄을 그대로 박스에 넣었다 ("한카리아스의 지진 → 상대 또가스 100% → 45%").
로그 문법이지 게임 자막이 아니었다.

**이제:** 프로토콜 한 줄이 **로그용 `text`와 자막용 `msg`로 갈라진다.** 자막은 실기 문구를 따른다.

| 상황 | 자막 |
|---|---|
| 내 포켓몬 출전 | `가랏! 한카리아스!` |
| 상대 출전 | `단델은
마기라스를 내보냈다!` |
| 기술 | `상대 마기라스의
스톤에지!` |
| 날씨 피해 | `모래바람이 리자몽을
덮쳤다!` ← 참고 이미지의 그 문장 |
| 상태이상 | `리자몽은
화상을 입었다!` |
| 행동 불가 | `리자몽은 몸이 저려서
움직일 수 없다!` |
| 랭크 변화 | `한카리아스의 공격이
크게 올라갔다!` |
| 기절 | `상대 마기라스는
쓰러졌다!` |

- **2줄 + 타자기 효과** — 실기처럼 한 글자씩 찍히고, 다 찍힌 뒤 읽을 시간을 준다
- **한국어 조사 자동 처리** — 받침 유무로 은/는, 을/를, 이/가를 계산한다
  (리자몽**을** / 한카리아스**를**, 리자몽**은** / 마기라스**는**)

### 배속 — 재생 도중 변경이 안 되던 문제

원인: `playBattleLog`가 호출 시점에 배속 값을 **한 번만** 받아서 끝까지 그 값을 썼다.
값을 읽어가는 **함수**를 넘기도록 바꿔서 매 단계 다시 읽는다.

실측: 1배속 8.9초 / 2배속 4.8초 / 4배속 2.9초 / 즉시 0.01초.
**1배속으로 시작해 1.2초 뒤 4배속으로 바꾸니 8.9초짜리가 4.4초에 끝났다.**

옵션 값도 "밀리초"에서 "배수"로 바꾸고 0.7배속을 추가했다. 기본값은 1배속(이전엔 2배속).

### 특성 표시 — 팝업 박스 제거

5세대 실기는 특성을 **자막으로** 알린다. 쇼다운식 팝업 박스는 실기에 없는 것이라 걷어내고,
자막(`상대 마기라스의
모래날림!`) + 해당 포켓몬이 잠깐 반짝이는 연출로 바꿨다.

### HP 박스 — 실기 배치

- 이름 + **성별 기호(♂/♀)** + Lv
- **"HP" 라벨**이 붙은 바 (실기의 노란 HP 글자)
- **내 포켓몬만 실수치(41/183) 노출** — 상대는 바만 (실기와 동일)
- 상태이상 배지(잠듦/화상/독…), 랭크 변화 표기

### 이펙트 — 외부 에셋 없이 CSS로

- **타입별로 연출이 갈린다**: 불꽃/물/전기/에스퍼 등은 시전 모션 + 날아가는 탄,
  노말/격투/바위/강철 등 물리 계열은 몸으로 부딪히는 돌진
- 피격 시 타입 색 폭발 + 튀는 입자 + 화면 플래시
- **날씨가 화면 전체에 적용된다** — 모래바람이면 배경이 모래색으로 바뀌고 사선 입자가 흐른다
- 출전 시 등장 모션, 기절 시 아래로 사라짐

### 그 외

- 관전 창 [닫기]가 재생을 실제로 중단한다 (이전엔 뒤에서 계속 돌았다)

---

## 최근 작업 3: 특성 팝업 + 스프라이트 소스 교체 + 포켓로그 에셋 검토

### (1) 특성 발동 팝업 — 완료

쇼다운처럼 특성이 터질 때 화면에 박스가 뜬다. **에셋 없이 순수 CSS/HTML.**
프로토콜의 `|-ability|`는 이미 파싱되고 있었고 로그에만 찍히던 것을 화면으로 끌어냈다.

- `protocol-ko.js`: `-ability`에 `{k:'ability', side, ability, mon}` 표시 지시 추가
- `battle-view.js`: `abilityPopup()` — 해당 진영 쪽에 박스를 띄우고 1.6초 뒤 사라짐
- `index.html`: `.abilitybox` 스타일 + `abpop` 키프레임

확인: `<b>부유</b><em>상대 또가스</em>` 박스가 상대 진영에 정상 렌더링.

### (2) 스프라이트 — BW 애니메이션으로 교체, 완료

**지시에 사실과 다른 부분이 하나 있었다:** "지금 PokeAPI의 `other/showdown/`을 쓰고 있다"고
하셨는데, 실제로는 `@pkmn/img`를 통해 **쇼다운 CDN의 gen5 정지 PNG**를 쓰고 있었다.
base64로 내장해둔 스프라이트도 없었다. (그래서 "다시 받아서 교체" 작업은 해당 없음)

바꾼 내용:
- 소스를 `PokeAPI/sprites`의 `versions/generation-v/black-white/animated/`로 교체.
  정지 → **애니메이션**이 되면서 실제 DS 원본 도트 느낌이 살아났다.
- 5세대 이후 종은 이 폴더에 없어서 `other/showdown/`으로 자동 폴백
  (실측 확인: 타부자고 #1000은 BW 404 → showdown 63x82 정상)
- 폼 변형도 정상 (`645-therian.gif`, `479-wash.gif` 실측 확인)
- `@pkmn/img` 의존 제거, `@pkmn/dex`로 도감번호+폼 접미사를 직접 만든다

**부수 효과 — `sprite-fit.js` 보정표가 불필요해졌다.**
예전 정지 스프라이트는 96x96 고정 캔버스라 종마다 여백이 달라 보정이 필요했는데,
BW 애니메이션 GIF는 **내용에 맞게 잘려 있다**(리자몽 87x89 / 해피너스 61x59).
그래서 "픽셀당 배율을 일정하게" 유지하는 방식으로 바꿨고, 덩치 차이가 자연스럽게 살아난다.
(실측: 레트라 57px → 박스 15.5%, 아보크 87px → 29.7%)

### (3) 포켓로그 배틀 애니메이션 — 보류했다가 **나중에 뒤집힘**

> 아래는 이 시점의 판단이다. 이후 사용자가 "어차피 전체 공개 코드로 할거라 그냥 진행해"라고
> 결정해서 **최근 작업 6**에서 실제로 도입했다. 사실관계 정정도 그쪽에 적어뒀다.

가져오지 않았다. 저장소를 실제로 확인한 결과:

**JSON 프레임 데이터** — `battle-anims/`에 924개 파일이 있는 건 맞다. 그런데
루트 `REUSE.toml`은 이걸 AGPL-3.0-only로 적어놨지만, **폴더 안에 자체 `REUSE.toml`이 또 있고
그게 우선한다.** 거기서 갈라지는 실제 내역:

| 라이선스 | 파일 수 | 비고 |
|---|---|---|
| `LicenseRef-POKEMON-REBORN` | **785개** | 포켓몬 리본 팀 저작물. flamethrower / swords-dance / earthquake / scratch 전부 여기 |
| `CC-BY-NC-SA-4.0` | 나머지 | **NC = 비상업 전용, SA = 동일조건 변경허락** |

**그래픽(`images/battle_anims/`)** — 이쪽이 진짜 문제다. 저장소의
`LICENSES/LicenseRef-FAIR-USE.txt`가 이렇게 적고 있다:

> "The asset is the intellectual property of Nintendo, Creatures, inc., and GAME FREAK, inc. ...
> The authors of this repository believe its inclusion is covered under Fair Use.
> **We do not claim to re-license nor hold any ownership over the asset.**"

즉 **이미지에는 넘겨받을 라이선스 자체가 없다.** 공정이용은 특정 사용에 대한 항변이지
남에게 넘길 수 있는 허락이 아니다. 출처를 표기해도 해결되지 않는 종류의 문제다.

덧붙여, AGPL인 부분도 "출처 표기"와는 성격이 다르다 — 네트워크로 서비스하면
(GitHub Pages가 여기 해당) 전체 소스 공개 의무가 붙는 강한 카피레프트다.

**대안:** 지난번에 정리한 CC0 경로가 그대로 유효하다. 프레임 데이터 방식 자체는 좋은 설계라
그대로 따르되, 시트만 CC0(Kenney / itch.io CC0 태그)로 채우면 된다. 재생 엔진은 우리가 짜면 되고,
`spriteUrl()`처럼 소스를 한 곳에 가둬두면 나중에 교체도 쉽다.

### 크레딧

`CREDITS.md` 신규 작성 — 코드 라이브러리 / 스프라이트 출처 / 폰트 / 보류한 리소스까지 정리.

---

## 최근 작업 2: 배틀 로그 혼동 + 온도 폭주

### "또가스가 두 번 출전하는 버그" — 실은 두 가지가 겹쳐 있었다

**(1) 중복 출전은 없었다.** 19경기 스캔 결과 같은 편이 같은 포켓몬을 연속 출전시킨 경우 **0건**.
실제로는 **양 팀에 같은 종족이 동시에 들어있던 것**(19경기 중 38건 — 풀이 36종뿐이라 6마리씩
뽑으면 겹치는 게 거의 필연). 로그가 소속을 구분해주지 않아 같은 개체로 읽혔다.

수정: 쇼다운 리플레이 방식대로 p2 쪽 포켓몬을 `상대 ○○`로 표기.
`박재이: 또가스 출전!` / `임민준: 상대 또가스 출전!`

**(2) 진짜 문제 — 온도 폭주로 리그 절반이 무작위로 뒀다.**

§4.2 곡선이 20 미만을 강하게 깎는데(판단력 14 → 실효 10.5), 그 부족분이 온도식의
1.7제곱으로 증폭돼 **T가 26~60까지** 치솟았다. T=55면 점수 차 40짜리 수를 겨우 2:1로
선호하는 수준 = 동전던지기. **리그 20명 중 13명이 그 구간**에 있었다.

| 판단력 | 실효(curve) | 온도 T |
|---|---|---|
| 20 | 20.0 | 1.20 |
| 17 | 14.9 | 9.6 |
| 14 | 10.5 | **26.4** |
| 10 | 5.7 | **51.6** |

SPEC §4.4는 "일반 트레이너 상단 구간 17~20"을 전제했는데, 내가 만든 소속사 스탯 범위가
8~20이라 대부분이 곡선의 붕괴 구간에 들어가 있었다.

수정: **온도 상한 14** (`MAX_TEMPERATURE`). "못 두는 것"과 "아무거나 두는 것"을 가른다.

**A/B 검증 (같은 시드)**

| 조건 | 상한없음 | 상한14 | 차이 |
|---|---|---|---|
| 올20 vs 올20 [SPEC 검증구간] | 56.0% | 56.0% | **0.0p** |
| 올20 vs 네임드25 [SPEC 검증구간] | 79.5% | 79.5% | **0.0p** |
| 스탯14 vs 14 평균 턴 | 43.8턴 | 23.4턴 | -20.4턴 |

SPEC이 검증한 구간은 T=1.2~1.9라 상한에 아예 닿지 않는다. 무는 건 16 이하뿐.

**1턴 교체는 버그가 아니었다.** 상성이 개선되는 교체가 30~47%, 악화는 3~10%.
나쁜 리드 매치업에서 빠지는 정상적인 플레이다. 상한을 걸면 오히려 더 좋아진다(개선 38%, 악화 3%).

---

## 최근 작업 1: 브래킷/관전 불일치 버그 수정 + SPEC v4 반영

### 버그 — 원인이 SPEC의 추정과 달랐다

**증상:** 브래킷에는 민재가 올라갔는데 관전하면 재이가 이긴다.

**SPEC §14-6의 추정:** "승자/패자 변수 대입이 뒤바뀌었을 가능성. 기존 대회 결과가 오염됐을 수 있으니 리셋 여부 판단할 것."

**실제 원인 (다름):** 변수 대입은 멀쩡했다. 문제는 **관전이 배틀을 처음부터 다시 돌리고 있었다**는 것.
`runMatch()`가 트레이너의 *현재* 상태를 매번 새로 읽었는데, 대회가 끝난 뒤 그 값들이 계속 변한다:

- 대회 참가로 컨디션 -30 → `effectiveStats()`가 다른 값을 낸다
- 이후 훈련/배틀로 스탯이 자란다

그래서 관전은 "그때 그 경기"가 아니라 **지금 능력치로 붙는 새 경기**였다.
실측: 20일치 55경기 중 승자 불일치 4건, 턴 수는 그보다 훨씬 자주 어긋남(51턴 → 49턴 등).

**수정:** 경기 시점의 입력(실효 스탯·팀·스타일)을 매치 기록에 스냅샷으로 굳히고,
재생은 그 스냅샷으로만 한다 (`replayMatch()`). 팀은 문자열이라 참조만 잡혀 메모리 부담 없음.

**중요 — 데이터 오염은 없었다.** `test/bracket-integrity.mjs`로 확인:
대회 10개 67경기에서 (1) 승자는 항상 참가자 중 하나 (2) loserId는 항상 승자의 반대편
(3) 다음 라운드 진출자는 전부 직전 라운드 승자 (4) 우승자 = 결승 승자.
**따라서 SPEC §14-6이 우려한 "기존 기록 리셋"은 불필요하다.** 거짓말을 한 건 관전 화면뿐이었다.

**회귀 테스트 2개 추가**
- `test/replay-consistency.mjs` — 브래킷 기록 == 관전 결과 (승자·턴수 모두)
- `test/bracket-integrity.mjs` — 브래킷 내부 일관성

### SPEC v4 반영 — 지금 빌드와의 격차

SPEC.md를 v4로 갱신했다. 신규 3개 섹션이 전부 현재 빌드와 직접 충돌하거나 빈 자리를 지적한다:

| v4 신규 | 내용 | 현재 빌드 상태 |
|---|---|---|
| **§3.10 오프닝 시퀀스** | 트레이너 **1명**으로 시작 → 뱃지 8개 → 그때서야 로스터 확장/유스 개방 | ❌ **정면 충돌**. 지금은 4명으로 시작하고 1일차부터 영입 가능 |
| **§5.6 연간 시즌 사이클** | 시즌 전반부(뱃지/로컬컵) → 후반부(지방리그) → 월즈 → 스토브리그 | ❌ 없음. 날짜가 무한히 흐르기만 함 |
| **§7.6 아이템(회복약)** | 엔진에 가방 개념이 없어 별도 후처리 필요 | ❌ 미구현 (기존에 보고한 그대로) |

§13 우선순위도 갱신됐다 — **3.5(브래킷 버그 수정) → 3.6(오프닝 시퀀스)** 가 다음 순서로 명시됨.
3.5는 이번에 끝냈으므로 **다음은 §3.10 오프닝 시퀀스**다.

### §14-7 자발적 교체 버그에 대해

v4에도 남아 있는데, 지난 세션에 조사해서 **의심하신 원인(벤치 평가 시 상대 랭크업 미반영)은 아님**을 숫자로 확인했다
(`getStat`이 boosts를 정상 반영, +2 칼춤 후 해피너스 피격량 107.5%로 정확히 계산됨).
재현이 안 됐던 이유는 그 배틀의 시드가 없어서였는데, **이번 스냅샷 수정으로 모든 경기가 정확히 재현 가능해졌다** —
이제 이상한 교체가 보이면 그 경기를 그대로 다시 돌려 판단 근거를 찍어볼 수 있다.

---

## 다음 우선순위 (SPEC §13 기준)

1. **§3.10 오프닝 시퀀스** — 이게 게임 시작 구조를 통째로 바꾼다
   - 로스터 1명으로 시작, 뱃지 8개 전까지 영입/유스 잠금
   - 관동·성도 체육관 관장 16명 데이터화 (원작 이름·타입·포켓몬)
   - 맵/리스트 UI로 체육관 도전 (§0.1-1에 예외 허용 명시됨)
2. **§5.6 시즌 사이클** — 지금 "목표가 파산 회피뿐"인 문제의 해답
3. §7.6 아이템(회복약) — `@pkmn/sim` 배틀 상태 직접 조작 가능 여부 조사부터

## 판단 필요

- **기존 세이브/진행 데이터**: 오염은 없으니 리셋 불필요. 다만 §3.10을 넣으면 시작 구조가 바뀌므로
  그때는 새로 시작해야 한다.
- SPEC v4 §3.10 문단에 깨진 글자 하나 있음 ("6~7번째 **�드지** 구간부터") — 원문 수정 요망.

---

## 현재 구현 상태

**작동하는 것**
- 하루 단위 루프: 행동 배정(훈련/휴식/대회 출전) → [다음 날] → 대회·성장·경비·뉴스
- 컨디션(대회 -30) / 자금 압박(일일 경비) / PA-CA 성장 / 스카웃 시장(영입·방출)
- 대회 등급 3종(하급/중급/상급)과 레이팅 밴드 출전 자격
- NPC 소속사 4곳이 §3.5 로직으로 자체 판단해 참가
- 내 트레이너 경기 관전 (이제 기록과 100% 일치)

**스키마만 있고 로직 없음 (§0.2 원칙)**
- 코치 · 계약 협상/바이아웃 · 글로벌 대회 인원 제한 · 경량 계층(타지방)
- 전설의 포켓몬 · NPC 성장/적응 · 콘테스트 · 회복약
- 뱃지(`badgeProgress`)·유스(`youthStatus`) — v4에서 새로 요구된 필드, 아직 없음
