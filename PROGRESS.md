# 진행 상황 — 관동·성도 리그 1개 돌리기

> 이 파일만 보면 뭘 끝냈고 다음에 뭘 해야 하는지 알 수 있게 유지한다.
> 세션이 언제 끊겨도 여기서 이어받을 수 있어야 함.

**세션 목표:** 관동·성도 리그 하나가 실제로 돌아가게 만들기. 완결·타지역은 범위 밖.

---

## 상태 요약

| # | 항목 | 상태 |
|---|---|---|
| 1 | 배틀 길이 튜닝 (§13-3단계) | ✅ 완료 |
| 2 | 소속사 시스템 — 정밀 계층, 리그 1개 (§13-7단계, §3) | ✅ 완료 |
| 3 | 최소 UI (순위표 / 로스터 / 배틀 로그) | ✅ 완료 |

---

## 판단이 필요한 질문 (임의로 정하지 않고 기본값으로 진행 중)

### Q1. 배틀 길이 목표치 — 지시가 상충함
요청에 "**목표: 길이 상관 없음**"과 "필요한 걸 동원해서 **이 범위에 맞춰줘**"가 같이 적혀 있는데,
"이 범위"가 가리키는 수치가 빠져 있습니다. (SPEC §7.4의 원래 목표는 평균 8~12턴)

- **기본값으로 진행 중:** 턴 수 자체를 목표로 잡지 않고, §13-3단계의 원래 취지인
  "**배틀이 충분히 길어져서 기점기·집중력·멘탈이 실제로 발동하는가**"를 합격 기준으로 삼음.
  (6마리 파티에서 이미 평균 21.9턴 / 기점기 배틀당 0.77~1.00회 발동 확인됨 — 4턴짜리 2v2에서는 0회였음)
- 8~12턴으로 **줄이는** 게 목표였다면 알려주세요. 그 경우 6마리 → 4마리로 줄이거나
  내구 투자를 빼는 방향으로 다시 맞추면 됩니다.

### Q2. 리그 소속사별 트레이너 수
SPEC에 명시 없음. **기본값: 소속사당 4명** (대기업 5명 / 중소 3명 등 등급별 차등 없이 시작).
→ 정밀 계층 4~5개 소속사 × 4명 = 16~20명. 로컬 대회 16강 브래킷이 딱 맞음.

### Q3. 로컬 대회 개최 주기 / 시즌 길이
SPEC §5.2는 "잦음"이라고만 되어 있음. **기본값: 7일마다 1회, 시즌 = 12주(84일)**.

### Q4. 상금·평판 배수 (§6.4 화폐 설계의 "기준 단위 100")
**기본값(대회 등급별로 3단계로 확장함):**

| 등급 | 레이팅 밴드 | 참가비 | 우승 | 준우승 | 4강 |
|---|---|---|---|---|---|
| 하급 | ~15 | 30 | 300 / 평판 6 | 120 / 3 | 60 / 1 |
| 중급 | 12~21 | 50 | 800 / 평판 12 | 300 / 6 | 150 / 3 |
| 상급 | 18~ | 80 | 2000 / 평판 25 | 800 / 12 | 400 / 6 |

### Q5. 로켓단이 12주 동안 우승 0회 — 의도인지 확인 필요
로켓단은 §3.6의 "조무래기 양성" 컨셉대로 `grunt` 프로필(약한 종족 + 노력치 거의 무투자)이라
평균 레이팅 10.8로 리그 최하위입니다. 하급 대회엔 꾸준히 나가지만(23승 48패) 우승은 0회.

- **컨셉대로면 맞는 결과**입니다 — 개체는 약하되 수로 밀어붙이는 조직.
- 다만 "12주 내내 무관"이 너무 가혹하면, ① 로스터 인원을 다른 소속사보다 늘리거나
  (수적 우위 = 컨셉과 일치) ② statRange 하한을 8→10으로 올리는 정도로 조정 가능합니다.
- 지금은 **컨셉 우선으로 두고 손대지 않았습니다.**

---

## 1. 배틀 길이 튜닝 — ✅ 완료

**한 것**
- `src/data/pokemon-pool.js` — 36종 포켓몬 풀. tier(종족 강함) + role(노력치 배분처) 태그.
  36종 전부 gen9 덱스 대조 검증 통과 (`node test/validate-pool.mjs`).
- `src/engine/team-builder.js` — 풀에서 6마리를 뽑아 팀을 조립. 등급 프로필 5종
  (elite/strong/mid/weak/grunt)이 "어느 tier를 뽑을지"와 "노력치를 얼마나 투자할지"를 함께 결정.
  → 노력치가 소속사 육성 노하우를 표현하는 레버가 됨 (§3.6 "무지개=자금은 있지만 육성 노하우 약함").
- `test/battle-tuning.mjs` — 합격 확인 스크립트.

**결과 (조건당 60회)**

| 조건 | 평균 턴 | 기점기 | 집중력 발동 | 멘탈 발동 |
|---|---|---|---|---|
| §14 기존 2마리 무투자 | 4.4턴 | **0%** | 불가(구조적) | 거의 없음 |
| elite vs elite | 18.2턴 | 배틀의 43% | 46% | 19% |
| mid vs mid | 21.4턴 | 배틀의 35% | 54% | 16% |
| grunt vs grunt | 19.8턴 | 배틀의 25% | 51% | 17% |

**두 축이 각각 독립적으로 승률을 움직이는 것 확인** — 리그가 성립하려면 이게 필수:
- 팀 전력 축: elite vs grunt 100% / elite vs mid 90% / mid vs weak 80%
- 트레이너 실력 축(팀 동일): 만점 vs 약체(7) 93.3% / vs 중간(14) 86.7% / vs 만점 58.3%

**성능:** 6v6 배틀 1회 ≈ 20ms. 16강 브래킷(15배틀) ≈ 0.4초, 12주 시즌 전체 ≈ 4초.

> 참고: 평균 18~21턴으로 SPEC §7.4의 원래 목표(8~12턴)보다 깁니다. Q1 참고 —
> 줄이는 게 목표였다면 말씀해주세요.

## 2. 소속사 시스템 — ✅ 완료

**한 것**
- `src/data/agencies.js` — 소속사/트레이너 스키마를 **SPEC 최종 형태로** 정의 (§0.2 원칙).
  코치·계약·바이아웃·PA/CA·천성 필드는 지금부터 존재하되 로직은 비어 있음.
  관동·성도 라인업은 §3.6 확정분 그대로 + 플레이어 소속사(자체).
- `src/engine/league.js` — 리그 생성 / §3.5 참가 판단 / 싱글 엘리미네이션 / 상금·평판 지급 /
  순위표 / 뉴스 피드. 매치마다 시드를 저장해서 **나중에 같은 배틀을 그대로 재생**할 수 있음
  (로그를 통째로 들고 있지 않아도 됨).
- `test/league-run.mjs` — 12주 시즌 검증 + 부전승 버그 회귀 확인.

**중간에 잡은 버그 2개**
1. **부전승 버그** — 시드를 앞에서부터 채우고 남는 자리를 빈칸으로 두니 *최하위 시드가
   빈칸만 만나며 결승까지 직행*했다 (0승 0패인데 준우승 12회). 표준 시드 순서로 교체.
   회귀 테스트를 test에 넣어둠.
2. **죽은 리그** — 처음엔 로켓단·도라지가 12주 내내 한 번도 출전을 안 했다. 참가 판단의
   "대회 평균 스탯"을 리그 전체 평균으로 잡아서 하급 대회에서도 실프 기준으로 승산을
   계산했기 때문. SPEC §3.5에 답이 이미 있었음 — *"스탯 낮은 NPC는 하위 대회만"* —
   대회를 **등급 3종(하급/중급/상급)**으로 나누고 레이팅 밴드를 출전 자격으로 걸었다.

**결과 (12주 / 36개 대회 / 288경기 / 8.2초)**

| 대회 등급 | 자격자 | 우승 분포 |
|---|---|---|
| 하급 (레이팅 ~15) | 11명 (로켓단·도라지·신생) | 신생 7회, 도라지 5회 |
| 중급 (12~21) | 11명 (무지개·도라지·신생) | 무지개 11회, 신생 1회 |
| 상급 (18~) | 5명 (실프 4 + 무지개 1) | 실프 12회 |

최종 순위: 실프(평판 667) > 무지개(298) > 신생★(132) > 도라지(103) > 로켓단(48)
→ 등급이 높은 소속사가 상위 대회를 독식하되, 하위 대회에서는 약소 소속사가 우승을 나눠 갖는
   구조가 실제로 나옴. §3.5가 노린 "참가 자체가 자연스러운 필터링"이 작동.

## 3. 최소 UI — ✅ 완료

기존 패턴(`.panel` / `table` / `.note` / `button`)만 사용. 새 디자인 없음.

**한 것**
- `index.html`에 탭 2개 추가: **배틀 시뮬** / **관동·성도 리그**. 기존 패널은 전부
  `data-panel="sim"`으로 묶고 리그 패널을 새로 붙였다.
- `src/ui/battle-view.js` — 관전 화면(스프라이트/HP바/이펙트/로그 재생)을 main.js에서
  분리. **두 탭이 같은 관전 화면을 공유**한다. 옮긴 코드라 동작 변화 없음(회귀 확인함).
- `src/ui/league-view.js` — 순위표 / 로스터 / 대회·경기 목록 / 소식.

**동작**
- 순위표: 소속사별 평판·자금·우승·승패. **행 클릭 → 로스터**(트레이너별 5스탯,
  레이팅, 전적, 파티 6마리)
- 대회 목록 클릭 → 경기 목록 → **관전 버튼**을 누르면 배틀 시뮬 탭으로 넘어가
  그 경기가 그대로 재생된다. 저장된 시드로 다시 돌리는 방식이라 결과가 기록과 일치.
- 버튼: 다음 주 진행 / 시즌 끝까지(12주) / 새 리그

**부수 작업:** `src/data/ko.js` 한글 표를 풀 전체로 확장 (종족 37 · 기술 73 · 특성 27 ·
타입 표기 37). 안 그러면 리그 로그가 영문으로 나온다.

**확인:** 두 탭 모두 콘솔 에러 0, 프로덕션 빌드 통과, 13주 시즌 UI에서 실행 확인.

---

---

## 다음에 이어서 할 때

**바로 이어붙일 수 있는 것 (스키마는 이미 있음)**
- §13-4 집중력·지식·멘탈·연패 페널티 **검증** — 배틀이 길어졌으니 이제 검증 가능.
  `test/battle-tuning.mjs`에 계측이 이미 들어 있어서 그걸 확장하면 됨.
- §13-5 천성 연결 — `trainer.nature.style`에 값은 이미 들어 있고, `league.js`의
  `runMatch()`가 `a.nature.style`을 그대로 AI에 넘기고 있다. 즉 **데이터만 다양하게
  넣으면 바로 작동**한다 (지금은 전원 '균형형' 고정).
- §13-6 육성 루프 — `potential`/`current` 필드는 있고 성장 로직만 없음.
- §13-9 계약 — `trainer.contract` 필드 전부 있음.
- §5.2 글로벌 대회 — `tournament.perAgencyLimit` 필드 있음(로컬은 null).

**알려진 한계**
- 회복약(가방 아이템) 미구현 — @pkmn/sim에 없는 개념. 운영 스탯이 교체 판단에만 걸림.
- 시즌이 끝나도 리셋/승격강등 없음. 계속 누적되기만 함.
- 트레이너 스탯 고정 — 성장/노쇠 없음.

---

## 이번 세션에서 의도적으로 안 건드린 것 (SPEC §0.2 — 스키마만 만들고 로직은 비움)

- 천성(순응도·스타일) 연결, 육성 루프(PA/CA)
- 코치 시스템, 계약/바이아웃/경질
- 글로벌 대회, 경량 계층(타지방)
- 전설의 포켓몬, NPC 성장/적응
- 콘테스트/포켓우드
- 회복약(가방 아이템) — @pkmn/sim에 없는 개념이라 별도 후처리 필요 (§6.4 미정)
